.\" -*- mode: troff; coding: utf-8 -*-
.\" Automatically generated by Pod::Man v6.0.2 (Pod::Simple 3.45)
.\" Translated to Chinese by Aurolystant for man-pages-zh project, 2026-07-13.
.\" Upstream version: gcc-16, dated 2026-06-25 (from .TH line).
.\"
.\" Standard preamble:
.\" ========================================================================
.de Sp \" Vertical space (when we can't use .PP)
.if t .sp .5v
.if n .sp
..
.de Vb \" Begin verbatim text
.ft CW
.nf
.ne \\$1
..
.de Ve \" End verbatim text
.ft R
.fi
..
.\" \*(C` and \*(C' are quotes in nroff, nothing in troff, for use with C<>.
.ie n \{\
.    ds C` ""
.    ds C' ""
'br\}
.el\{\
.    ds C`
.    ds C'
'br\}
.\"
.\" Escape single quotes in literal strings from groff's Unicode transform.
.ie \n(.g .ds Aq \(aq
.el       .ds Aq '
.\"
.\" If the F register is >0, we'll generate index entries on stderr for
.\" titles (.TH), headers (.SH), subsections (.SS), items (.Ip), and index
.\" entries marked with X<> in POD.  Of course, you'll have to process the
.\" output yourself in some meaningful fashion.
.\"
.\" Avoid warning from groff about undefined register 'F'.
.de IX
..
.nr rF 0
.if \n(.g .if rF .nr rF 1
.if (\n(rF:(\n(.g==0)) \{\
.    if \nF \{\
.        de IX
.        tm Index:\\$1\t\\n%\t"\\$2"
..
.        if !\nF==2 \{\
.            nr % 0
.            nr F 2
.        \}
.    \}
.\}
.rr rF
.\"
.\" Required to disable full justification in groff 1.23.0.
.if n .ds AD l
.\" ========================================================================
.\"
.IX Title "CPP 1"
.TH CPP 1 2026-06-25 gcc-16 GNU
.\" For nroff, turn off justification.  Always turn off hyphenation; it makes
.\" way too many mistakes in technical documents.
.if n .ad l
.nh
.SH 名称
cpp \- C 预处理器
.SH 总览
.IX Header "总览"
cpp [\fB\-D\fR\fImacro\fR[=\fIdefn\fR]...] [\fB\-U\fR\fImacro\fR]
    [\fB\-I\fR\fIdir\fR...] [\fB\-iquote\fR\fIdir\fR...]
    [\fB\-M\fR|\fB\-MM\fR] [\fB\-MG\fR] [\fB\-MF\fR \fIfilename\fR]
    [\fB\-MP\fR] [\fB\-MQ\fR \fItarget\fR...]
    [\fB\-MT\fR \fItarget\fR...]
    \fIinfile\fR [[\fB\-o\fR] \fIoutfile\fR]
.PP
上面仅列出了最有用的选项；有关预处理器特有选项的完整列表，请参见下文。
此外，\fBcpp\fR 还接受大多数 \fBgcc\fR 驱动程序选项，此处未列出。
详情请参阅 GCC 文档。
.SH 描述
.IX Header "描述"
C 预处理器（通常称为 \fIcpp\fR）是一个\fI宏处理器\fR，
由 C 编译器自动使用，用于在编译之前对程序进行转换。
它被称为宏处理器，因为它允许您定义\fI宏（macros）\fR，
宏是较长构造的简短缩写。
.PP
C 预处理器旨在仅用于 C、C++ 和 Objective\-C 源代码。
过去，它曾被滥用作通用文本处理器。
对于不遵守 C 词法规则的输入，它将无法处理。
例如，撇号将被解释为字符常量的开头，并导致错误。
此外，您不能依赖它保留对 C 系列语言不重要的输入特性。
如果对 Makefile 进行预处理，所有硬制表符都将被删除，Makefile 将无法工作。
.PP
话虽如此，您通常还是可以侥幸将 cpp 用于非 C 语言。
其他类 Algol 编程语言通常是安全的（Ada 等）。
汇编语言在谨慎使用下也是安全的。
\&\fB\-traditional\-cpp\fR 模式会保留更多空白，并且在其他方面更加宽松。
许多问题可以通过编写 C 或 C++ 风格的注释而不是使用原生语言注释，
以及保持宏简单来避免。
.PP
只要可能，您应该使用针对您所编写语言的预处理器。
现代版本的 GNU 汇编器具有宏功能。
大多数高级编程语言都有自己的条件编译和包含机制。
如果所有这些都失败了，请尝试真正的通用文本处理器，例如 GNU M4。
.PP
C 预处理器在某些细节上有所不同。本文档讨论 GNU C 预处理器，
它提供了 ISO 标准 C 功能的一个小超集。
在其默认模式下，GNU C 预处理器没有执行标准要求的少数几件事。
这些是很少（如果有的话）使用的功能，并且可能会导致不期望它们的程序的含义发生出乎意料的更改。
要获得严格的 ISO 标准 C，您应该根据所需的标准版本使用
\&\fB\-std=c90\fR、\fB\-std=c99\fR、\fB\-std=c11\fR 或 \fB\-std=c17\fR 选项。
要获得所有强制性诊断，您还必须使用 \fB\-pedantic\fR。
.PP
本文档描述了 ISO 预处理器的行为。
为了尽量减少不必要的差异，在 ISO 预处理器的行为与传统语义不冲突的地方，
传统预处理器应以相同的方式工作。
确实存在的各种差异在\fB传统模式\fR部分中有详细说明。
.PP
为清晰起见，除非另有说明，本文档中引用 \fBCPP\fR 均指 GNU CPP。
.SH 选项
.IX Header "选项"
\&\fBcpp\fR 命令需要两个文件名作为参数：\fIinfile\fR 和 \fIoutfile\fR。
预处理器读取 \fIinfile\fR 以及它通过 \fB#include\fR 指定的任何其他文件。
所有组合输入文件生成的输出都写入 \fIoutfile\fR。
.PP
\&\fIinfile\fR 或 \fIoutfile\fR 都可以是 \fB\-\fR，
作为 \fIinfile\fR 表示从标准输入读取，作为 \fIoutfile\fR 表示写入标准输出。
如果任一文件被省略，其效果等同于为该文件指定了 \fB\-\fR。
您也可以使用 \fB\-o\fR \fIoutfile\fR 选项来指定输出文件。
.PP
除非另有说明，或者选项以 \fB=\fR 结尾，所有接受参数的选项都可以让参数紧跟在选项之后，
或者在选项和参数之间有一个空格：
\&\fB\-Ifoo\fR 和 \fB\-I foo\fR 效果相同。
.PP
许多选项有多字母名称；因此多个单字母选项\fI不能\fR组合：
\&\fB\-dM\fR 与 \fB\-d\ \-M\fR 差别很大。
.IP "\fB\-D\fR \fIname\fR" 4
.IX Item "-D name"
.PD 0
.IP \fB\-\-define\-macro=\fR\fIname\fR 4
.IX Item "--define-macro=name"
.IP "\fB\-\-define\-macro\fR \fIname\fR" 4
.IX Item "--define-macro name"
.PD
将 \fIname\fR 预定义为宏，定义为 \f(CW1\fR。
.IP "\fB\-D\fR \fIname\fR\fB=\fR\fIdefinition\fR" 4
.IX Item "-D name=definition"
.PD 0
.IP \fB\-\-define\-macro=\fR\fIname\fR\fB=\fR\fIdefinition\fR 4
.IX Item "--define-macro=name=definition"
.IP "\fB\-\-define\-macro\fR \fIname\fR\fB=\fR\fIdefinition\fR" 4
.IX Item "--define-macro name=definition"
.PD
\&\fIdefinition\fR 的内容被词法分析并处理，就像它们在翻译阶段三出现在 \fB#define\fR 指令中一样。
特别地，定义会被嵌入的换行符截断。
.Sp
如果您从 shell 或类似 shell 的程序调用预处理器，
您可能需要使用 shell 的引号语法来保护在 shell 语法中有特殊含义的字符（如空格）。
.Sp
如果您希望在命令行上定义类似函数的宏，
请在等号（如果有）之前用括号括起其参数列表。
括号对大多数 shell 都有意义，因此您应该对选项加引号。
对于 \fBsh\fR 和 \fBcsh\fR，
\&\fB\-D\*(Aq\fR\fIname\fR\fB(\fR\fIargs...\fR\fB)=\fR\fIdefinition\fR\fB\*(Aq\fR 有效。
.Sp
\&\fB\-D\fR 和 \fB\-U\fR 选项按照它们在命令行上给出的顺序进行处理。
所有 \fB\-imacros\fR \fIfile\fR 和 \fB\-include\fR \fIfile\fR 选项在所有 \fB\-D\fR 和 \fB\-U\fR 选项之后处理。
.IP "\fB\-U\fR \fIname\fR" 4
.IX Item "-U name"
.PD 0
.IP \fB\-\-undefine\-macro=\fR\fIname\fR 4
.IX Item "--undefine-macro=name"
.IP "\fB\-\-undefine\-macro\fR \fIname\fR" 4
.IX Item "--undefine-macro name"
.PD
取消 \fIname\fR 的任何先前定义，无论是内建的还是通过 \fB\-D\fR 选项提供的。
.IP "\fB\-include\fR \fIfile\fR" 4
.IX Item "-include file"
.PD 0
.IP \fB\-\-include=\fR\fIfile\fR 4
.IX Item "--include=file"
.IP "\fB\-\-include\fR \fIfile\fR" 4
.IX Item "--include file"
.PD
处理 \fIfile\fR，就像 \f(CW\*(C`#include "file"\*(C'\fR 作为主源文件的第一行出现一样。
但是，搜索 \fIfile\fR 的第一个目录是预处理器的\fI工作目录\fR，而不是包含主源文件的目录。
如果在那里未找到，则像往常一样在 \f(CW\*(C`#include "..."\*(C'\fR 搜索链的其余部分中搜索。
.Sp
如果给出了多个 \fB\-include\fR 选项，则文件按照它们在命令行上出现的顺序包含。
.IP "\fB\-imacros\fR \fIfile\fR" 4
.IX Item "-imacros file"
.PD 0
.IP \fB\-\-imacros=\fR\fIfile\fR 4
.IX Item "--imacros=file"
.IP "\fB\-\-imacros\fR \fIfile\fR" 4
.IX Item "--imacros file"
.PD
与 \fB\-include\fR 完全相同，只是扫描 \fIfile\fR 产生的任何输出都被丢弃。
宏它定义的宏仍然保持定义状态。
这允许您从头文件中获取所有宏，而无需处理其声明。
.Sp
所有由 \fB\-imacros\fR 指定的文件在所有由 \fB\-include\fR 指定的文件之前处理。
.IP \fB\-undef\fR 4
.IX Item "-undef"
不要预定义任何系统特定或 GCC 特定的宏。
标准预定义宏仍保持定义状态。
.IP \fB\-pthread\fR 4
.IX Item "-pthread"
定义使用 POSIX 线程库所需的额外宏。
您应该在编译和链接时一致地使用此选项。
此选项在 GNU/Linux 目标、大多数其他 Unix 衍生系统以及 x86 Cygwin 和 MinGW 目标上受支持。
.IP \fB\-M\fR 4
.IX Item "-M"
.PD 0
.IP \fB\-\-dependencies\fR 4
.IX Item "--dependencies"
.PD
不输出预处理结果，而是输出适用于 \fBmake\fR 的规则，描述主源文件的依赖关系。
预处理器输出一条 \fBmake\fR 规则，包含该源文件的目标文件名、冒号以及所有包含文件
（包括来自 \fB\-include\fR 或 \fB\-imacros\fR 命令行选项的包含文件）的名称。
.Sp
除非明确指定（使用 \fB\-MT\fR 或 \fB\-MQ\fR），目标文件名由源文件的名称组成，
其任何后缀被替换为目标文件后缀，并删除任何前导目录部分。
如果包含许多文件，则规则将使用 \fB\e\fR\-换行符分成多行。
该规则没有命令。
.Sp
此选项不会抑制预处理器的调试输出，例如 \fB\-dM\fR。
为了避免将此类调试输出与依赖规则混合，您应该使用 \fB\-MF\fR 明确指定依赖输出文件，
或使用诸如 \fBDEPENDENCIES_OUTPUT\fR 之类的环境变量。
调试输出仍然像往常一样发送到常规输出流。
.Sp
将 \fB\-M\fR 传递给驱动程序会隐含 \fB\-E\fR，并通过隐式的 \fB\-w\fR 抑制警告。
.IP \fB\-MM\fR 4
.IX Item "-MM"
.PD 0
.IP \fB\-\-user\-dependencies\fR 4
.IX Item "--user-dependencies"
.PD
类似于 \fB\-M\fR，但不提及在系统头文件目录中找到的头文件，
也不提及直接或间接从这样的头文件中包含的头文件。
.Sp
这意味着在 \fB#include\fR 指令中使用尖括号还是双引号本身并不决定
该头文件是否出现在 \fB\-MM\fR 依赖输出中。
.IP "\fB\-MF\fR \fIfile\fR" 4
.IX Item "-MF file"
当与 \fB\-M\fR 或 \fB\-MM\fR 一起使用时，指定一个用于写入依赖关系的文件。
如果没有给出 \fB\-MF\fR 开关，预处理器将规则发送到与发送预处理输出相同的位置。
.Sp
当与驱动程序选项 \fB\-MD\fR 或 \fB\-MMD\fR 一起使用时，
\&\fB\-MF\fR 会覆盖默认的依赖输出文件。
.Sp
如果 \fIfile\fR 是 \fI\-\fR，则依赖关系写入 \fI标准输出\fR。
.IP \fB\-MG\fR 4
.IX Item "-MG"
.PD 0
.IP \fB\-\-print\-missing\-file\-dependencies\fR 4
.IX Item "--print-missing-file-dependencies"
.PD
与请求依赖生成的选项（如 \fB\-M\fR）结合使用时，
\&\fB\-MG\fR 假定缺失的头文件是生成的文件，并将其添加到依赖列表中而不引发错误。
依赖文件名直接从 \f(CW\*(C`#include\*(C'\fR 指令中获取，而不添加任何路径。
\&\fB\-MG\fR 也会抑制预处理输出，因为缺失的头文件使其无用。
.Sp
此功能用于 makefile 的自动更新。
.IP \fB\-Mno\-modules\fR 4
.IX Item "-Mno-modules"
禁用已编译模块接口的依赖生成。
.IP \fB\-MP\fR 4
.IX Item "-MP"
此选项指示 CPP 为每个依赖项（主文件除外）添加一个假目标，
使每个依赖项不依赖于任何东西。
这些伪规则可以解决在您删除头文件而未更新 \fIMakefile\fR 时 \fBmake\fR 报出的错误。
.Sp
典型的输出如下：
.Sp
.Vb 1
\&        test.o: test.c test.h
\&
\&        test.h:
.Ve
.IP "\fB\-MT\fR \fItarget\fR" 4
.IX Item "-MT target"
更改依赖生成所发出规则的目标。
默认情况下，CPP 获取主输入文件的名称，删除任何目录组件和任何文件后缀（如 \fB.c\fR），
并附加平台通常的目标文件后缀。结果即为目标。
.Sp
\&\fB\-MT\fR 选项将目标设置为您指定的确切字符串。
如果您想要多个目标，可以将它们指定为 \fB\-MT\fR 的单个参数，
或使用多个 \fB\-MT\fR 选项。
.Sp
例如，\fB\-MT\ \*(Aq$(objpfx)foo.o\*(Aq\fR 可能给出
.Sp
.Vb 1
\&        $(objpfx)foo.o: foo.c
.Ve
.IP "\fB\-MQ\fR \fItarget\fR" 4
.IX Item "-MQ target"
与 \fB\-MT\fR 相同，但它会对 Make 中特殊的任何字符进行引号处理。
\&\fB\-MQ\ \*(Aq$(objpfx)foo.o\*(Aq\fR 给出
.Sp
.Vb 1
\&        $$(objpfx)foo.o: foo.c
.Ve
.Sp
默认目标会自动添加引号，就像使用 \fB\-MQ\fR 给出的一样。
.IP \fB\-MD\fR 4
.IX Item "-MD"
.PD 0
.IP \fB\-\-write\-dependencies\fR 4
.IX Item "--write-dependencies"
.PD
\&\fB\-MD\fR 等效于 \fB\-M \-MF\fR \fIfile\fR，只是不隐含 \fB\-E\fR。
驱动程序根据是否给出了 \fB\-o\fR 选项来确定 \fIfile\fR。
如果给出了，驱动程序使用其参数但后缀为 \fI.d\fR，
否则获取输入文件的名称，删除任何目录组件和后缀，并应用 \fI.d\fR 后缀。
.Sp
如果 \fB\-MD\fR 与 \fB\-E\fR 结合使用，任何 \fB\-o\fR 开关都被理解为指定依赖输出文件，
但如果与 \fB\-E\fR 一起使用，每个 \fB\-o\fR 都被理解为指定目标目标文件。
.Sp
由于不隐含 \fB\-E\fR，\fB\-MD\fR 可用于在编译过程中作为副作用生成依赖输出文件。
.IP \fB\-MMD\fR 4
.IX Item "-MMD"
.PD 0
.IP \fB\-\-write\-user\-dependencies\fR 4
.IX Item "--write-user-dependencies"
.PD
类似于 \fB\-MD\fR，但只提用户头文件，不提系统头文件。
.IP \fB\-fpreprocessed\fR 4
.IX Item "-fpreprocessed"
向预处理器指示输入文件已被预处理。
这抑制了宏展开、三字符组转换、转义换行符拼接以及大多数指令的处理。
预处理器仍然识别并删除注释，以便您可以将用 \fB\-C\fR 预处理过的文件传递给编译器而不会出现问题。
在此模式下，集成的预处理器只不过是为前端提供词法分析的工具。
.Sp
如果输入文件的扩展名是 \fB.i\fR、\fB.ii\fR 或 \fB.mi\fR，则 \fB\-fpreprocessed\fR 是隐含的。
这些是 GCC 用于 \fB\-save\-temps\fR 创建的预处理文件的扩展名。
.IP \fB\-fdirectives\-only\fR 4
.IX Item "-fdirectives-only"
预处理时，处理指令但不展开宏。
.Sp
该选项的行为取决于 \fB\-E\fR 和 \fB\-fpreprocessed\fR 选项。
.Sp
使用 \fB\-E\fR 时，预处理仅限于处理 \f(CW\*(C`#define\*(C'\fR、\f(CW\*(C`#ifdef\*(C'\fR 和 \f(CW\*(C`#error\*(C'\fR 等指令。
其他预处理器操作，如宏展开和三字符组转换，不会执行。
此外，\fB\-dD\fR 选项被隐式启用。
.Sp
使用 \fB\-fpreprocessed\fR 时，命令行和大多数内建宏的预定义被禁用。
像 \f(CW\*(C`_\|_LINE_\|_\*(C'\fR 这样依赖于上下文的宏会被正常处理。
这允许编译之前用 \f(CW\*(C`\-E \-fdirectives\-only\*(C'\fR 预处理过的文件。
.Sp
同时使用 \fB\-E\fR 和 \fB\-fpreprocessed\fR 时，\fB\-fpreprocessed\fR 的规则优先。
这允许对之前用 \f(CW\*(C`\-E \-fdirectives\-only\*(C'\fR 预处理过的文件进行完全预处理。
.IP \fB\-fdollars\-in\-identifiers\fR 4
.IX Item "-fdollars-in-identifiers"
接受标识符中的 \fB$\fR 字符。
.IP \fB\-fextended\-identifiers\fR 4
.IX Item "-fextended-identifiers"
接受标识符中的通用字符名称和扩展字符。
此选项在 C99（及更高 C 标准版本）和 C++ 中默认为启用。
.IP \fB\-fno\-canonical\-system\-headers\fR 4
.IX Item "-fno-canonical-system-headers"
预处理时，不要通过规范化来缩短系统头文件路径。
.IP \fB\-fmax\-include\-depth=\fR\fIdepth\fR 4
.IX Item "-fmax-include-depth=depth"
设置嵌套 #include 的最大深度。默认为 200。
.IP \fB\-fsearch\-include\-path\fR[\fB=\fR\fIkind\fR] 4
.IX Item "-fsearch-include-path[=kind]"
在 #include 路径上查找输入文件，而不仅仅在当前目录。
这对于 C++20 模块特别有用，因为头文件单元和模块接口单元都需要直接编译：
.Sp
.Vb 1
\&        g++ \-c \-std=c++20 \-fmodules \-fsearch\-include\-path bits/stdc++.h bits/std.cc
.Ve
.Sp
\&\fIkind\fR 默认为 \fBuser\fR，在 \f(CW#include "..."\fR 搜索路径上查找；
您也可以明确指定 \fBsystem\fR 用于 \f(CW\*(C`#include <...>\*(C'\fR 搜索路径。
.IP \fB\-ftabstop=\fR\fIwidth\fR 4
.IX Item "-ftabstop=width"
设置制表符停止位之间的距离。
这有助于预处理器在警告或错误中报告正确的列号，即使行中出现制表符。
如果值小于 1 或大于 100，该选项将被忽略。默认为 8。
.IP \fB\-ftrack\-macro\-expansion\fR[\fB=\fR\fIlevel\fR] 4
.IX Item "-ftrack-macro-expansion[=level]"
追踪宏展开过程中词法单元的位置。
这允许编译器在宏展开中发生编译错误时，输出关于当前宏展开栈的诊断信息。
使用此选项会使预处理器和编译器消耗更多内存。
\&\fIlevel\fR 参数可用于选择词法单元位置追踪的精度级别，
从而在必要时减少内存消耗。\fIlevel\fR 值 \fB0\fR 停用此选项。
值 \fB1\fR 以降级模式追踪词法单元位置，以最小化内存开销。
在此模式下，类似函数的宏的参数展开产生的所有词法单元具有相同的位置。
值 \fB2\fR 完全追踪词法单元位置。此值最消耗内存。
当此选项未带参数给出时，默认参数值为 \fB2\fR。
.Sp
注意，\f(CW\*(C`\-ftrack\-macro\-expansion=2\*(C'\fR 默认处于激活状态。
.IP \fB\-fmacro\-prefix\-map=\fR\fIold\fR\fB=\fR\fInew\fR 4
.IX Item "-fmacro-prefix-map=old=new"
当预处理位于目录 \fIold\fR 中的文件时，
展开 \f(CW\*(C`_\|_FILE_\|_\*(C'\fR 和 \f(CW\*(C`_\|_BASE_FILE_\|_\*(C'\fR 宏时，如同文件位于目录 \fInew\fR 中。
这可以用于将绝对路径更改为相对路径，通过对 \fInew\fR 使用 \fI.\fR 来实现，
这可以产生更可重现且位置无关的构建。
此选项在编译期间也影响 \f(CW\*(C`_\|_builtin_FILE()\*(C'\fR。
另请参见 \fB\-ffile\-prefix\-map\fR 和 \fB\-fcanon\-prefix\-map\fR。
.IP \fB\-fexec\-charset=\fR\fIcharset\fR 4
.IX Item "-fexec-charset=charset"
设置执行字符集，用于字符串和字符常量。默认值为 UTF\-8。
\&\fIcharset\fR 可以是系统 \f(CW\*(C`iconv\*(C'\fR 库例程支持的任何编码。
.IP \fB\-fwide\-exec\-charset=\fR\fIcharset\fR 4
.IX Item "-fwide-exec-charset=charset"
设置宽执行字符集，用于宽字符串和宽字符常量。
默认值为 UTF\-32BE、UTF\-32LE、UTF\-16BE 或 UTF\-16LE 之一，
具体取决于 \f(CW\*(C`wchar_t\*(C'\fR 的宽度以及用于代码生成的大端或小端字节序。
与 \fB\-fexec\-charset\fR 一样，\fIcharset\fR 可以是系统 \f(CW\*(C`iconv\*(C'\fR 库例程支持的任何编码；
但是，对于不能完全放入 \f(CW\*(C`wchar_t\*(C'\fR 的编码，您会遇到问题。
.IP \fB\-finput\-charset=\fR\fIcharset\fR 4
.IX Item "-finput-charset=charset"
设置输入字符集，用于从输入文件的字符集转换到 GCC 使用的源字符集。
默认值为 UTF\-8。\fIcharset\fR 可以是系统 \f(CW\*(C`iconv\*(C'\fR 库例程支持的任何编码。
.Sp
如果输入字符集是 UTF\-8，并且启用了 \fB\-Winvalid\-utf8\fR，则会发出关于格式错误的代码单元序列的警告。
否则，当输入字符集与执行字符集匹配时，不会发出诊断信息。
如果它们不同，格式错误的代码单元序列会在转码到执行字符集时导致错误。
.IP \fB\-fworking\-directory\fR 4
.IX Item "-fworking-directory"
启用预处理器输出中行标记的生成，让编译器知道预处理时当前工作目录。
启用此选项时，预处理器在初始行标记之后，发出第二个行标记，
包含当前工作目录后跟两个斜杠。
GCC 在预处理器输入中存在此目录时，将其用作某些调试信息格式中作为当前工作目录发出的目录。
如果启用了调试信息，此选项会被隐式启用，
但可以通过否定形式 \fB\-fno\-working\-directory\fR 来抑制。
如果命令行中存在 \fB\-P\fR 标志，此选项无效，
因为根本不发出任何 \f(CW\*(C`#line\*(C'\fR 指令。
.IP "\fB\-A\fR \fIpredicate\fR\fB=\fR\fIanswer\fR" 4
.IX Item "-A predicate=answer"
.PD 0
.IP "\fB\-\-assert\fR \fIpredicate\fR\fB=\fR\fIanswer\fR" 4
.IX Item "--assert predicate=answer"
.IP \fB\-\-assert=\fR\fIpredicate\fR\fB=\fR\fIanswer\fR 4
.IX Item "--assert=predicate=answer"
.PD
使用谓词 \fIpredicate\fR 和答案 \fIanswer\fR 做出断言。
此形式优于仍受支持的旧形式 \fB\-A\fR \fIpredicate\fR\fB(\fR\fIanswer\fR\fB)\fR，
因为它不使用 shell 特殊字符。
.Sp
断言是预处理器的过时功能，仅为了与某些目标上的传统系统头文件兼容而受支持。
.IP "\fB\-A \-\fR\fIpredicate\fR\fB=\fR\fIanswer\fR" 4
.IX Item "-A -predicate=answer"
.PD 0
.IP "\fB\-\-assert \-\fR\fIpredicate\fR\fB=\fR\fIanswer\fR" 4
.IX Item "--assert -predicate=answer"
.IP \fB\-\-assert=\-\fR\fIpredicate\fR\fB=\fR\fIanswer\fR 4
.IX Item "--assert=-predicate=answer"
.PD
取消具有谓词 \fIpredicate\fR 和答案 \fIanswer\fR 的断言。
.IP \fB\-C\fR 4
.IX Item "-C"
.PD 0
.IP \fB\-\-comments\fR 4
.IX Item "--comments"
.PD
不要丢弃注释。所有注释都传递到输出文件，
但已处理指令中的注释除外，它们会与指令一起被删除。
.Sp
使用 \fB\-C\fR 时，您应该为副作用做好准备；
它使预处理器将注释视为独立的词法单元。
例如，出现在本应是指令行开头的注释，
其效果是将该行变成普通的源代码行，
因为该行的第一个词法单元不再是 \fB#\fR。
.IP \fB\-CC\fR 4
.IX Item "-CC"
.PD 0
.IP \fB\-\-comments\-in\-macros\fR 4
.IX Item "--comments-in-macros"
.PD
不要丢弃注释，包括在宏展开期间。
这类似于 \fB\-C\fR，但宏中包含的注释也会在宏展开时传递到输出文件。
.Sp
除了 \fB\-C\fR 选项的副作用之外，\fB\-CC\fR 选项还会导致宏内所有的 C++ 风格注释转换为 C 风格注释。
这是为了防止以后使用该宏时意外地注释掉源代码行的其余部分。
.Sp
\&\fB\-CC\fR 选项通常用于支持 lint 注释。
.IP \fB\-P\fR 4
.IX Item "-P"
.PD 0
.IP \fB\-\-no\-line\-commands\fR 4
.IX Item "--no-line-commands"
.PD
禁止在预处理器输出中生成行标记。
当在非 C 代码上运行预处理器，并且输出将发送到可能被行标记混淆的程序时，这可能很有用。
.IP \fB\-traditional\fR 4
.IX Item "-traditional"
.PD 0
.IP \fB\-\-traditional\fR 4
.IX Item "--traditional"
.IP \fB\-traditional\-cpp\fR 4
.IX Item "-traditional-cpp"
.IP \fB\-\-traditional\-cpp\fR 4
.IX Item "--traditional-cpp"
.PD
尝试模仿标准化前 C 预处理器的行为，而不是 ISO C 预处理器。
.Sp
注意，GCC 本身并不试图模仿标准化前的 C 编译器，
并且这些选项仅在 \fB\-E\fR 开关下或在显式调用 CPP 时受支持。
.IP \fB\-trigraphs\fR 4
.IX Item "-trigraphs"
.PD 0
.IP \fB\-\-trigraphs\fR 4
.IX Item "--trigraphs"
.PD
支持 ISO C 三字符组。
这些是三字符序列，均以 \fB??\fR 开头，
由 ISO C 定义为代表单个字符。
例如，\fB??/\fR 代表 \fB\e\fR，
因此 \fB\*(Aq??/n\*(Aq\fR 是换行符的字符常量。
.Sp
默认情况下，GCC 忽略三字符组，
但在符合标准模式下会进行转换。
请参见 \fB\-std\fR 和 \fB\-ansi\fR 选项。
.IP \fB\-remap\fR 4
.IX Item "-remap"
启用特殊代码以解决仅允许非常短文件名的文件系统（如 MS\-DOS）的问题。
.IP \fB\-H\fR 4
.IX Item "-H"
.PD 0
.IP \fB\-\-trace\-includes\fR 4
.IX Item "--trace-includes"
.PD
打印所使用的每个头文件的名称，除其他正常活动外。
每个名称都缩进以显示其在 \fB#include\fR 栈中的深度。
预编译头文件也会被打印，即使它们被发现无效；
无效的预编译头文件用 \fB...x\fR 打印，有效的用 \fB...!\fR 打印。
.IP \fB\-d\fR\fIletters\fR 4
.IX Item "-dletters"
.PD 0
.IP \fB\-\-dump=\fR\fIletters\fR 4
.IX Item "--dump=letters"
.IP "\fB\-\-dump\fR \fIletters\fR" 4
.IX Item "--dump letters"
.PD
指定在编译期间按照 \fIletters\fR 进行调试转储。
此处记录的是与预处理器相关的标志。
其他 \fIletters\fR 由编译器本身解释或保留给 GCC 的未来版本，因此被静默忽略。
如果您指定了行为冲突的 \fIletters\fR，则结果未定义。
.RS 4
.IP \fB\-dM\fR 4
.IX Item "-dM"
.PD 0
.IP \fB\-\-dump=M\fR 4
.IX Item "--dump=M"
.PD
不生成正常输出，而是为预处理器执行期间定义的所有宏生成 \fB#define\fR 指令列表，
包括预定义宏。这为您提供了一种查找预处理器版本中预定义了哪些宏的方法。
假设您没有文件 \fIfoo.h\fR，命令
.Sp
.Vb 1
\&        touch foo.h; cpp \-dM foo.h
.Ve
.Sp
将显示所有预定义的宏。
.IP \fB\-dD\fR 4
.IX Item "-dD"
.PD 0
.IP \fB\-\-dump=D\fR 4
.IX Item "--dump=D"
.PD
类似于 \fB\-dM\fR，但输出\fI同时\fR包含 \fB#define\fR 指令和预处理结果。
两种输出都进入标准输出文件。
.IP \fB\-dN\fR 4
.IX Item "-dN"
.PD 0
.IP \fB\-\-dump=N\fR 4
.IX Item "--dump=N"
.PD
类似于 \fB\-dD\fR，但仅输出宏名称，不输出其展开。
.IP \fB\-dI\fR 4
.IX Item "-dI"
.PD 0
.IP \fB\-\-dump=I\fR 4
.IX Item "--dump=I"
.PD
在预处理结果之外，额外输出 \fB#include\fR 指令。
.IP \fB\-dU\fR 4
.IX Item "-dU"
.PD 0
.IP \fB\-\-dump=U\fR 4
.IX Item "--dump=U"
.PD
类似于 \fB\-dD\fR，但仅输出被展开的宏或其在预处理器指令中进行了存在性测试的宏；
输出延迟到宏被使用或测试时；
对于被测试但在当时未定义的宏，也会输出 \fB#undef\fR 指令。
.RE
.RS 4
.RE
.IP \fB\-fdebug\-cpp\fR 4
.IX Item "-fdebug-cpp"
此选项仅对调试 GCC 有用。
当从 CPP 或使用 \fB\-E\fR 时，它会转储关于位置映射的调试信息。
输出中的每个词法单元之前都有其位置所属映射的转储。
.Sp
当从 GCC 使用而不带 \fB\-E\fR 时，此选项无效。
.IP "\fB\-I\fR \fIdir\fR" 4
.IX Item "-I dir"
.PD 0
.IP "\fB\-iquote\fR \fIdir\fR" 4
.IX Item "-iquote dir"
.IP "\fB\-isystem\fR \fIdir\fR" 4
.IX Item "-isystem dir"
.IP "\fB\-idirafter\fR \fIdir\fR" 4
.IX Item "-idirafter dir"
.IP \fB\-\-include\-directory\-after=\fR\fIdir\fR 4
.IX Item "--include-directory-after=dir"
.IP "\fB\-\-include\-directory\-after\fR \fIdir\fR" 4
.IX Item "--include-directory-after dir"
.IP \fB\-\-include\-directory=\fR\fIdir\fR 4
.IX Item "--include-directory=dir"
.IP "\fB\-\-include\-directory\fR \fIdir\fR" 4
.IX Item "--include-directory dir"
.PD
将目录 \fIdir\fR 添加到预处理期间要搜索头文件的目录列表中。
\&\fB\-\-include\-directory\fR 是 \fB\-I\fR 的别名，
而 \fB\-\-include\-directory\-after\fR 是 \fB\-idirafter\fR 的别名。
.Sp
如果 \fIdir\fR 以 \fB=\fR 或 \f(CW$SYSROOT\fR 开头，
则 \fB=\fR 或 \f(CW$SYSROOT\fR 被替换为 sysroot 前缀；
请参见 \fB\-\-sysroot\fR 和 \fB\-isysroot\fR。
.Sp
用 \fB\-iquote\fR 指定的目录仅适用于指令的引号形式 \f(CW\*(C`#include\ "\fR\f(CIfile\fR\f(CW"\*(C'\fR。
用 \fB\-I\fR、\fB\-isystem\fR 或 \fB\-idirafter\fR 指定的目录
适用于 \f(CW\*(C`#include\ "\fR\f(CIfile\fR\f(CW"\*(C'\fR
和 \f(CW\*(C`#include\ <\fR\f(CIfile\fR\f(CW>\*(C'\fR 指令的查找。
.Sp
您可以在命令行上指定任意数量或组合的这些选项，以在多个目录中搜索头文件。
查找顺序如下：
.RS 4
.IP 1. 4
.IX Item "1."
对于包含指令的引号形式，首先搜索当前文件所在的目录。
.IP 2. 4
.IX Item "2."
对于包含指令的引号形式，按 \fB\-iquote\fR 选项在命令行上出现的从左到右顺序搜索指定的目录。
.IP 3. 4
.IX Item "3."
按照 \fB\-I\fR 选项指定的目录按从左到右顺序扫描。
.IP 4. 4
.IX Item "4."
按照 \fB\-isystem\fR 选项指定的目录按从左到右顺序扫描。
.IP 5. 4
.IX Item "5."
扫描标准系统目录。
.IP 6. 4
.IX Item "6."
按照 \fB\-idirafter\fR 选项指定的目录按从左到右顺序扫描。
.RE
.RS 4
.Sp
您可以使用 \fB\-I\fR 覆盖系统头文件，替换为您自己的版本，
因为这些目录在标准系统头文件目录之前被搜索。
但是，您不应使用此选项添加包含供应商提供的系统头文件的目录；
应使用 \fB\-isystem\fR 来实现。
.Sp
\&\fB\-isystem\fR 和 \fB\-idirafter\fR 选项也将目录标记为系统目录，
使其获得与标准系统目录相同的特殊处理。
.Sp
如果标准系统包含目录或通过 \fB\-isystem\fR 指定的目录也通过 \fB\-I\fR 指定，
则 \fB\-I\fR 选项被忽略。
该目录仍在搜索，但作为系统目录处于其在系统包含链中的正常位置。
这是为了确保 GCC 修复有问题的系统头文件的程序以及 \f(CW\*(C`#include_next\*(C'\fR 指令的顺序不会意外更改。
如果您确实需要更改系统目录的搜索顺序，请使用 \fB\-nostdinc\fR 和/或 \fB\-isystem\fR 选项。
.RE
.IP \fB\-I\-\fR 4
.IX Item "-I-"
.PD 0
.IP \fB\-\-include\-barrier\fR 4
.IX Item "--include-barrier"
.PD
分割包含路径。
此选项已被弃用。请改用 \fB\-iquote\fR 代替 \fB\-I\-\fR 之前的 \fB\-I\fR 目录，
并移除 \fB\-I\-\fR 选项。
.Sp
在 \fB\-I\-\fR 之前通过 \fB\-I\fR 选项指定的任何目录
仅搜索使用 \f(CW\*(C`#include\ "\fR\f(CIfile\fR\f(CW"\*(C'\fR 请求的头文件；
它们不会用于搜索 \f(CW\*(C`#include\ <\fR\f(CIfile\fR\f(CW>\*(C'\fR。
如果在 \fB\-I\-\fR 之后通过 \fB\-I\fR 选项指定了其他目录，
这些目录将用于所有 \fB#include\fR 指令。
.Sp
此外，\fB\-I\-\fR 会禁止使用当前文件目录作为 \f(CW\*(C`#include\ "\fR\f(CIfile\fR\f(CW"\*(C'\fR 的第一个搜索目录。
无法覆盖 \fB\-I\-\fR 的这个效果。
.IP "\fB\-iprefix\fR \fIprefix\fR" 4
.IX Item "-iprefix prefix"
.PD 0
.IP \fB\-\-include\-prefix=\fR\fIprefix\fR 4
.IX Item "--include-prefix=prefix"
.IP "\fB\-\-include\-prefix\fR \fIprefix\fR" 4
.IX Item "--include-prefix prefix"
.PD
指定 \fIprefix\fR 作为后续 \fB\-iwithprefix\fR 选项的前缀。
如果前缀代表一个目录，您应该包含末尾的 \fB/\fR。
.IP "\fB\-iwithprefix\fR \fIdir\fR" 4
.IX Item "-iwithprefix dir"
.PD 0
.IP "\fB\-iwithprefixbefore\fR \fIdir\fR" 4
.IX Item "-iwithprefixbefore dir"
.IP \fB\-\-include\-with\-prefix=\fR\fIprefix\fR 4
.IX Item "--include-with-prefix=prefix"
.IP "\fB\-\-include\-with\-prefix\fR \fIprefix\fR" 4
.IX Item "--include-with-prefix prefix"
.IP \fB\-\-include\-with\-prefix\-after=\fR\fIprefix\fR 4
.IX Item "--include-with-prefix-after=prefix"
.IP "\fB\-\-include\-with\-prefix\-after\fR \fIprefix\fR" 4
.IX Item "--include-with-prefix-after prefix"
.IP \fB\-\-include\-with\-prefix\-before=\fR\fIprefix\fR 4
.IX Item "--include-with-prefix-before=prefix"
.IP "\fB\-\-include\-with\-prefix\-before\fR \fIprefix\fR" 4
.IX Item "--include-with-prefix-before prefix"
.PD
将 \fIdir\fR 附加到之前用 \fB\-iprefix\fR 指定的前缀，
并将结果目录添加到包含搜索路径。
\&\fB\-iwithprefixbefore\fR 将其放在 \fB\-I\fR 会放置的位置；
\&\fB\-iwithprefix\fR 将其放在 \fB\-idirafter\fR 会放置的位置。
.Sp
\&\fB\-\-include\-with\-prefix\fR 和 \fB\-\-include\-with\-prefix\-after\fR 都是 \fB\-iwithprefix\fR 的别名，
而 \fB\-\-include\-with\-prefix\-before\fR 是 \fB\-iwithprefixbefore\fR 的别名。
.IP "\fB\-isysroot\fR \fIdir\fR" 4
.IX Item "-isysroot dir"
此选项类似于 \fB\-\-sysroot\fR 选项，但仅适用于头文件
（Darwin 目标除外，它同时适用于头文件和库）。更多信息请参见 \fB\-\-sysroot\fR 选项。
.IP "\fB\-imultilib\fR \fIdir\fR" 4
.IX Item "-imultilib dir"
将 \fIdir\fR 用作包含目标特定 C++ 头文件的目录的子目录。
.IP "\fB\-imultiarch\fR \fIdir\fR" 4
.IX Item "-imultiarch dir"
将 \fIdir\fR 用作包含架构特定 C++ 头文件的目录的子目录。
.IP \fB\-nostdinc\fR 4
.IX Item "-nostdinc"
.PD 0
.IP \fB\-\-no\-standard\-includes\fR 4
.IX Item "--no-standard-includes"
.PD
不在标准系统目录中搜索头文件。
仅搜索通过 \fB\-I\fR、\fB\-iquote\fR、\fB\-isystem\fR 和/或 \fB\-idirafter\fR 选项
（以及适当时当前文件的目录）明确指定的目录。
.IP \fB\-nostdinc++\fR 4
.IX Item "-nostdinc++"
不在 C++ 特定的标准目录中搜索头文件，
但仍搜索其他标准目录。（此选项在构建 C++ 库时使用。）
.IP \fB\-\-embed\-dir=\fR\fIdir\fR 4
.IX Item "--embed-dir=dir"
.PD 0
.IP \fB\-\-embed\-directory=\fR\fIdir\fR 4
.IX Item "--embed-directory=dir"
.IP "\fB\-\-embed\-directory\fR \fIdir\fR" 4
.IX Item "--embed-directory dir"
.PD
将 \fIdir\fR 目录附加到 \f(CW\*(C`#embed\*(C'\fR 预处理指令或 \f(CW\*(C`_\|_has_embed\*(C'\fR 宏的搜索目录列表中。
\&\f(CW\*(C`#embed\*(C'\fR 没有默认目录。
.Sp
如果 \fIdir\fR 以 \fB=\fR 或 \f(CW$SYSROOT\fR 开头，则 \fB=\fR 或 \f(CW$SYSROOT\fR 被替换为 sysroot 前缀；
请参见 \fB\-\-sysroot\fR 和 \fB\-isysroot\fR。
.IP \fB\-Wcomment\fR 4
.IX Item "-Wcomment"
.PD 0
.IP \fB\-Wcomments\fR 4
.IX Item "-Wcomments"
.PD
只要注释开始序列 \fB/*\fR 出现在 \fB/*\fR 注释中，
或者反斜杠\-换行符出现在 \fB//\fR 注释中，就发出警告。
此警告由 \fB\-Wall\fR 启用。
.IP \fB\-Wtrigraphs\fR 4
.IX Item "-Wtrigraphs"
如果遇到可能改变程序含义的三字符组，则发出警告。
注释内的三字符组不会发出警告，除非它们会形成转义换行符。
.Sp
此选项由 \fB\-Wall\fR 隐含启用。如果没有给出 \fB\-Wall\fR，
除非启用了三字符组，否则此选项仍然启用。
要启用三字符组转换而不发出警告，但获得其他 \fB\-Wall\fR 警告，
请使用 \fB\-trigraphs \-Wall \-Wno\-trigraphs\fR。
.IP \fB\-Wundef\fR 4
.IX Item "-Wundef"
如果未定义的标识符在 \f(CW\*(C`#if\*(C'\fR 指令中被求值，则发出警告。
此类标识符将被替换为零。
.IP \fB\-Wexpansion\-to\-defined\fR 4
.IX Item "-Wexpansion-to-defined"
只要在宏展开中遇到 \fBdefined\fR（包括宏被 \fB#if\fR 指令展开的情况），就发出警告。
此类用法不可移植。
此警告也由 \fB\-Wpedantic\fR 和 \fB\-Wextra\fR 启用。
.IP \fB\-Wunused\-macros\fR 4
.IX Item "-Wunused-macros"
对主文件中定义的未使用宏发出警告。
如果宏至少被展开或测试过一次存在性，则视为\fI已使用\fR。
预处理器还会在宏被重新定义或取消定义时，如果该宏尚未被使用，则发出警告。
.Sp
内建宏、命令行上定义的宏以及在包含文件中定义的宏不会发出警告。
.Sp
\&\fI注意：\fR 如果某个宏实际上被使用了，但仅在被跳过的条件块中使用，
那么预处理器会将其报告为未使用。要避免此类情况下的警告，
您可以改善宏定义的作用域，例如将其移到第一个被跳过的块中。
或者，您可以提供一个虚拟使用，例如：
.Sp
.Vb 2
\&        #if defined the_macro_causing_the_warning
\&        #endif
.Ve
.IP \fB\-Wno\-endif\-labels\fR 4
.IX Item "-Wno-endif-labels"
当 \f(CW\*(C`#else\*(C'\fR 或 \f(CW\*(C`#endif\*(C'\fR 后面跟着文本时，不要发出警告。
这有时出现在较老的程序中，代码如下：
.Sp
.Vb 5
\&        #if FOO
\&        ...
\&        #else FOO
\&        ...
\&        #endif FOO
.Ve
.Sp
第二个和第三个 \f(CW\*(C`FOO\*(C'\fR 应该在注释中。
此警告默认开启。
.SH 环境变量
.IX Header "环境变量"
本节描述影响 CPP 操作的环境变量。
您可以使用它们来指定搜索包含文件时要使用的目录或前缀，或控制依赖输出。
.PP
请注意，您也可以使用诸如 \fB\-I\fR 之类的选项指定搜索位置，
并使用诸如 \fB\-M\fR 之类的选项控制依赖输出。
这些选项优先于环境变量，而环境变量又优先于 GCC 的配置。
.IP \fBCPATH\fR 4
.IX Item "CPATH"
.PD 0
.IP \fBC_INCLUDE_PATH\fR 4
.IX Item "C_INCLUDE_PATH"
.IP \fBCPLUS_INCLUDE_PATH\fR 4
.IX Item "CPLUS_INCLUDE_PATH"
.IP \fBOBJC_INCLUDE_PATH\fR 4
.IX Item "OBJC_INCLUDE_PATH"
.PD
每个变量的值是一个由特殊字符分隔的目录列表，类似于 \fBPATH\fR，
用于在其中查找头文件。
特殊字符 \f(CW\*(C`PATH_SEPARATOR\*(C'\fR 是目标相关的，在 GCC 构建时确定。
对于基于 Microsoft Windows 的目标，它是分号；对于几乎所有其他目标，它是冒号。
.Sp
\&\fBCPATH\fR 指定要搜索的目录列表，就像用 \fB\-I\fR 指定一样，
但在命令行上通过 \fB\-I\fR 选项给出的任何路径之后。
此环境变量无论预处理哪种语言都会被使用。
.Sp
其余环境变量仅在对特定语言进行预处理时适用。
每个变量指定要搜索的目录列表，就像用 \fB\-isystem\fR 指定一样，
但在命令行上通过 \fB\-isystem\fR 选项给出的任何路径之后。
.Sp
在所有这些变量中，空元素指示编译器搜索其当前工作目录。
空元素可以出现在路径的开头或结尾。
例如，如果 \fBCPATH\fR 的值为 \f(CW\*(C`:/special/include\*(C'\fR，
则其效果与 \fB\-I.\ \-I/special/include\fR 相同。
.IP \fBDEPENDENCIES_OUTPUT\fR 4
.IX Item "DEPENDENCIES_OUTPUT"
如果设置了此变量，其值指定如何基于编译器处理的非系统头文件输出 Make 的依赖关系。
系统头文件在依赖输出中被忽略。
.Sp
\&\fBDEPENDENCIES_OUTPUT\fR 的值可以只是一个文件名，
此时 Make 规则写入该文件，从源文件名推测目标名称。
或者该值可以采用 \fIfile\fR\fB \fR\fItarget\fR 的形式，
此时规则写入文件 \fIfile\fR，并使用 \fItarget\fR 作为目标名称。
.Sp
换句话说，此环境变量等效于组合使用 \fB\-MM\fR 和 \fB\-MF\fR 选项，
并可选地加上一个 \fB\-MT\fR 开关。
.IP \fBSUNPRO_DEPENDENCIES\fR 4
.IX Item "SUNPRO_DEPENDENCIES"
此变量与 \fBDEPENDENCIES_OUTPUT\fR（见上文）相同，
只是系统头文件不被忽略，因此它隐含 \fB\-M\fR 而不是 \fB\-MM\fR。
但是，省略了主输入文件的依赖关系。
.IP \fBSOURCE_DATE_EPOCH\fR 4
.IX Item "SOURCE_DATE_EPOCH"
如果设置了此变量，其值指定一个 UNIX 时间戳，
用于替换 \f(CW\*(C`_\|_DATE_\|_\*(C'\fR 和 \f(CW\*(C`_\|_TIME_\|_\*(C'\fR 宏中的当前日期和时间，
从而使嵌入的时间戳变得可重现。
.Sp
\&\fBSOURCE_DATE_EPOCH\fR 的值必须是 UNIX 时间戳，
定义为自 1970 年 1 月 1 日 00:00:00 以来的秒数（不包括闰秒），以 ASCII 表示；
与 GNU/Linux 和其他支持 \f(CW\*(C`date\*(C'\fR 命令中 \f(CW%s\fR 扩展的系统上的 \f(CW\*(C`date +%s\*(C'\fR 输出相同。
.Sp
该值应为已知的时间戳，例如源代码或软件包的最后修改时间，
并应由构建过程设置。
.SH "参见"
.IX Header "参见"
\&\fBgpl\fR\|(7), \fBgfdl\fR\|(7), \fBfsf\-funding\fR\|(7),
\&\fBgcc\fR\|(1), 以及 \fIcpp\fR 和 \fIgcc\fR 的 Info 条目。
.SH 版权
.IX Header "版权"
Copyright (c) 1987\-2026 Free Software Foundation, Inc.
.PP
允许在 GNU 自由文档许可证（Free Documentation License）1.3 版或自由软件基金会发布的任何更高版本
的条款下复制、分发和/或修改本文档。
该许可证的副本包含在手册页 \fBgfdl\fR\|(7) 中。
本手册没有不变章节。封面文本为 (a)（见下文），封底文本为 (b)（见下文）。
.PP
(a) FSF 的封面文本是：
.PP
.Vb 1
\&     A GNU Manual
.Ve
.PP
(b) FSF 的封底文本是：
.PP
.Vb 3
\&     You have freedom to copy and modify this GNU Manual, like GNU
\&     software.  Copies published by the Free Software Foundation raise
\&     funds for GNU development.
.Ve
.SH "[中文版维护人]"
.B Aurolystant
.B Aurolystant
.SH "[中文版最新更新]"
.BR 2026.07.13
.SH "[中国 Linux 论坛中文手册页翻译计划]"
.BI https://github.com/man-pages-zh/manpages-zh
