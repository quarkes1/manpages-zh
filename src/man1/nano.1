.\" Copyright (C) 1999-2011, 2013-2026 Free Software Foundation, Inc.
.\"
.\" 本文档采用双重许可。您可以根据以下任一许可条款分发和/或修改它：
.\"
.\" * GNU 通用公共许可证，由自由软件基金会发布，版本 3 或（根据您的选择）任何更高版本。
.\"   您应该已随本程序收到一份 GNU 通用公共许可证副本。如果没有，请参阅
.\"   <https://www.gnu.org/licenses/>。
.\"
.\" * GNU 自由文档许可证，由自由软件基金会发布，版本 1.2 或（根据您的选择）任何更高版本，
.\"   无不变章节、无封面文本、无封底文本。
.\"   您应该已随本程序收到一份 GNU 自由文档许可证副本。如果没有，请参阅
.\"   <https://www.gnu.org/licenses/>。
.\"
.TH NANO 1 "2026年6月" "GNU nano 9.1"

.SH 名称
nano \- Nano 的另一个文本编辑器，灵感来自 Pico

.SH 概述
.B nano
.RI [选项] [[\fB+\fR 行号 [\fB,\fR 列号]] \fI文件\fR ]...
.br
.BR nano " [" \fI选项\fR "] [[" + [ crCR ]{ / | ? } \fI字符串\fR "] " \fI文件\fR ]...

.SH 描述
\fBnano\fR 是一个小巧友好的文本编辑器。它模仿了 Pico 的外观和操作方式，
但它是自由软件，并且实现了 Pico 缺少的一些功能，例如：打开多个文件、
逐行滚动、撤销/重做、语法着色、行号显示以及超长行软折行。
.sp
在命令行指定文件名时，可以通过在文件名前加上带加号（\fB+\fR）的行号
将光标放在特定行上，甚至可以通过逗号加上列号放在特定列上。
负数从文件或行的末尾开始计数。
.sp
可以通过在文件名前指定 \fB+/\fR 或 \fB+?\fR 后跟特定字符串，
将光标放在该字符串的第一次或最后一次出现处。
通过在 \fB+\fR 号后插入 \fBc\fR 和/或 \fBr\fR，
可以使字符串区分大小写和/或被解释为正则表达式。
这些搜索模式可以通过使用这些字母的大写形式来显式禁用：
\fBC\fR 和/或 \fBR\fR。
当字符串包含空格时，需要用引号括起来。
举例来说：要打开一个文件并将光标放在单词 "Foo" 的第一次出现处，
可以这样做：
.sp
.RS 4
.BI "nano +c/Foo " 文件
.RE
.sp
作为一个特殊情况：如果指定的是短横线（\fB\-\fR）而不是文件名，
\fBnano\fR 将从标准输入读取数据。

.SH 注意事项
自版本 9.0 起，所有行会水平同步滚动（当光标接近边缘时），仅滚动所需的最小量，
而不是只将当前行以大步幅跳跃滚动。
如果您更喜欢旧的行为，可以在命令行使用 \fB\-\-\%solo\fR
或在您的 \fInanorc\fR 中设置 \fBset \%solosidescroll\fR。
.sp
同样自版本 9.0 起，按键 \fBM\-<\fR 和 \fBM\->\fR
已被重新分配给 \fB\%scrollleft\fR（向左滚动）和 \fB\%scrollright\fR（向右滚动）功能。
如果您希望这些按键执行它们在 9.0 版本之前的功能，
请在您的 \fInanorc\fR 文件末尾添加以下两行：
.sp
.RS 4
.B bind M\-< prevbuf main
.br
.B bind M\-> nextbuf main
.RE
.sp
自版本 8.0 起，为了对新用户更友好，\fB^F\fR 开始向前搜索，
\fB^B\fR 开始向后搜索，\fBM\-F\fR 搜索下一个向前出现，
\fBM\-B\fR 搜索下一个向后出现。
如果您希望这些按键执行它们在 8.0 版本之前的功能，
请在您的 \fInanorc\fR 文件末尾添加以下四行：
.sp
.RS 4
.B bind ^F forward main
.br
.B bind ^B back main
.br
.B bind M\-F formatter main
.br
.B bind M\-B linter main
.RE
.sp

.SH 编辑
输入文本和在文件中移动非常直接：键入字母并使用正常的光标移动键即可。
命令通过 Control（^）键和 Alt 或 Meta（M\-）键输入。
键入 \fB^K\fR 删除当前行并将其放入剪切缓冲区。
连续的 \fB^K\fR 将所有删除行一起放入剪切缓冲区。
任何光标移动或执行任何其他命令都会导致下一次 \fB^K\fR
覆盖剪切缓冲区。
\fB^U\fR 将剪切缓冲区的当前内容粘贴到当前光标位置。
.sp
当需要剪切或复制更精确的文本片段时，您可以用 \fB^6\fR 标记其开始，
移动光标到其结束位置（标记的文本会高亮显示），
然后使用 \fB^K\fR 剪切它，或使用 \fBM\-6\fR 将其复制到剪切缓冲区。
您还可以使用 \fB^O\fR 将标记的文本保存到文件，
或使用 \fB^T^T\fR 进行拼写检查。
.sp
在某些终端上，也可以按住 Shift 键同时使用方向键来选择文本。
同时按住 Ctrl 或 Alt 键会增加步进幅度。
未按住 Shift 键的任何光标移动都会取消此类选择。
.sp
任何有效的 Unicode 码点都可以通过键入 \fBM\-V\fR
后跟该码点的十六进制数字插入到缓冲区中
（如果少于六位数字，则以 \fB<Space>\fR 或 \fB<Enter>\fR 结束）。
可以通过键入 \fBM\-V\fR 后跟相关按键来插入字面控制码
（\fB^J\fR 除外）。
.sp
屏幕底部的两行显示了一些重要命令；
内置帮助（\fB^G\fR）列出了所有可用的命令。
可以通过 \fInanorc\fR 文件更改默认的按键绑定 \[em] 请参阅
.BR nanorc (5)。

.SH 选项
.TP
.BR \-A ", " \-\-smarthome
使 Home 键更智能。当 Home 键在行中非空白字符的开头以外的任何位置被按下时，
光标会跳到该开头（向前或向后）。
如果光标已经在该位置，则跳到行的真正开头。
.TP
.BR \-B ", " \-\-backup
保存文件时，备份之前的版本，使用当前文件名加上波浪号（\fB\[ti]\fR）后缀。
.TP
.BI \-C " 目录\fR, " \-\-backupdir=目录
当备份已启用（\fB\-B\fR）时，不仅只创建一个备份文件，
而是每次保存文件时都创建并保留一个唯一编号的备份文件 \[em]
这些唯一编号的文件存储在指定的\fI\%目录\fR中。
.TP
.BR \-D ", " \-\-boldtext
在界面中使用粗体代替反白显示。
这可以通过在 \fInanorc\fR 文件中设置
\fB\%titlecolor\fR、\fB\%statuscolor\fR、\fB\%promptcolor\fR、
\fB\%minicolor\fR、\fB\%keycolor\fR、\fB\%numbercolor\fR
和/或 \fB\%selectedcolor\fR 选项来为特定元素覆盖。
请参阅 \fBnanorc\fR(5)。
.TP
.BR \-E ", " \-\-tabstospaces
将每个键入的制表符转换为空格——转换为该位置制表符所占用的空格数。
（注意：粘贴的制表符不会被转换。）
.TP
.BR \-F ", " \-\-multibuffer
默认将文件读入新缓冲区。
.TP
.BR \-G ", " \-\-locking
编辑文件时使用 vim 风格的文件锁定。
.TP
.BR \-H ", " \-\-historylog
保存最近一百个搜索字符串、替换字符串和执行过的命令，
以便在后续会话中轻松重用。
.TP
.BR \-I ", " \-\-ignorercfiles
不读取系统的 \fInanorc\fR 也不读取用户的 \fInanorc\fR。
.TP
.BI \-J " 数字\fR, " \-\-guidestripe=数字
在指定列绘制一条垂直线，以帮助判断文本的宽度。
（线条的颜色可以通过在 \fInanorc\fR 文件中设置
\fBset \%stripecolor\fR 来更改。）
.TP
.BR \-K ", " \-\-rawsequences
直接解释转义序列，而不是要求 \fI\%ncurses\fR 翻译它们。
（如果您需要此选项才能使某些按键正常工作，
这意味着所使用的 \fI\%terminfo\fR 终端描述
与终端的实际行为不完全匹配。
例如，当您通过 SSH 进入 BSD 机器时可能会发生这种情况。）
使用此选项会禁用 \fBnano\fR 的鼠标支持。
.TP
.BR \-L ", " \-\-nonewlines
当文本不以换行结尾时不自动添加换行。
（这可能导致您保存非 POSIX 文本文件。）
.TP
.BR \-M ", " \-\-trimblanks
当自动硬折行发生或文本被对齐时，修剪折行行尾部的空白。
.TP
.BR \-N ", " \-\-noconvert
禁用自动将文件从 DOS 格式转换为 Unix 格式。
.TP
.BR \-O ", " \-\-bookstyle
对齐时，将以空白字符开头的任何行视为段落的开始
（除非自动缩进已开启）。
.TP
.BR \-P ", " \-\-positionlog
对于最近 200 个文件，记录光标的最后位置，
并在重新打开此类文件时再次将光标放置在该位置。
同时保存和恢复任何锚点的位置。
.TP
.BI "\-Q """ 正则表达式\fB"\fR, " \-\-quotestr=""" 正则表达式 """"
设置用于匹配行中引用部分的正则表达式。
默认值为 "\fB^([\ \\t]*([!#%:;>|}]|//))+\fR"。
（注意 \fB\\t\fR 代表实际的制表符。）
这使得在编写电子邮件时可以重新对齐引用文本块，
以及在编写源代码时可以重新换行注释块。
.TP
.BR \-R ", " \-\-restricted
仅读写命令行上指定的\fI文件\fR（或文件）。
这意味着：不读写历史文件；不允许挂起；不允许拼写检查；
不允许在 shell 中执行任何命令；
不允许当前缓冲区以不同名称保存；
不允许追加或前置到任何文件；以及不创建备份文件。
.sp
作为例外，在指定的文件之前，会读取 \fInanorc\fR 文件，
以允许自定义。
如果不允许自定义，请同时使用 \fB\-I\fR（\fB\-\-\%ignorercfiles\fR）
选项。
.sp
当命令行上未指定\fI文件\fR时，受限模式允许您
将键入到新缓冲区的内容保存到任何名称下，
但不能覆盖现有文件。
如果需要限制此新文件的创建位置，请同时使用
\fB\-o\fR（\fB\-\-\%operatingdir\fR）选项。
.sp
受限模式也可以通过以 'r' 开头的名称调用 \fBnano\fR 来激活
（例如：\fBrnano\fR）。
.TP
.BR \-S ", " \-\-softwrap
将超过屏幕宽度的行显示为多个屏幕行。
（您可以通过同时使用 \fB\-\-\%atblanks\fR
使此软折行在空白字符处发生，而不是在屏幕边缘粗鲁地断开。）
.TP
.BI \-T " 数字\fR, " \-\-tabsize=数字
将制表符的大小（宽度）设置为 \fI\%数字\fR 列。
\fI\%数字\fR 的值必须大于 0。默认值为 \fB8\fR。
.TP
.BR \-U ", " \-\-quickblank
使状态栏消息在 1 次按键后消失，而不是 20 次按键后。
注意选项 \fB\-c\fR（\fB\-\-\%constantshow\fR）会覆盖此设置。
当 \fB\-\-\%minibar\fR 或 \fB\-\-\%zero\fR 生效时，
\fB\-\-\%quickblank\fR 使消息在 0.8 秒后消失，
而不是默认的 1.5 秒。
.
.TP
.BR \-V ", " \-\-version
显示当前版本号并退出。
.TP
.BR \-W ", " \-\-wordbounds
通过将标点字符视为单词的一部分来不同地检测单词边界。
.TP
.BI "\-X """ 字符\fB"\fR, " \-\-wordchars=""" 字符 """"
指定哪些其他字符（除了通常的字母数字字符之外）
应被视为单词的一部分。
使用此选项时，您可能希望省略 \fB\-W\fR（\fB\-\-\%wordbounds\fR）。
.TP
.BI \-Y " 名称\fR, " \-\-syntax=名称
从 \fInanorc\fR 文件中定义的语法中
指定要使用的语法高亮名称。
.TP
.BR \-Z ", " \-\-zap
让未修改的 Backspace 或 Delete 删除标记区域
（而不是删除单个字符，且不影响剪切缓冲区）。
.TP
.BR \-a ", " \-\-atblanks
进行软折行时，在空白字符处折行，
而不是总是在屏幕边缘折行。
.TP
.BR \-b ", " \-\-breaklonglines
当当前行变得过长时自动硬折行，
即当它宽于 \fB\-r\fR（\fB\-\-fill\fR）给定的值时。
\fB\-r\fR 的默认值是终端宽度减去八。
（此选项与 \fB\-w\fR（\fB\-\-\%nowrap\fR）相反——
最后给定的一个生效。）
.TP
.BR \-c ", " \-\-constantshow
持续在状态栏上报告光标位置。
注意这会覆盖选项 \fB\-U\fR（\fB\-\-\%quickblank\fR）。
.TP
.BR \-d ", " \-\-rebinddelete
不同地解释 Delete 和 Backspace 键，
以便 Backspace 和 Delete 都能正常工作。
仅当在您的系统上 Backspace 表现像 Delete
或 Delete 表现像 Backspace 时，才应使用此选项。
.TP
.BR \-e ", " \-\-emptyline
不使用标题栏下方的行，使其完全空白。
.TP
.BI \-f " 文件\fR, " \-\-rcfile=文件
仅读取此\fI文件\fR来设置 \fBnano\fR 的选项，
而不是同时读取系统级和用户的 \fInanorc\fR 文件。
.TP
.BR \-g ", " \-\-showcursor
使光标在文件浏览器中（将光标放在高亮项目上）
和帮助查看器中可见。对视障用户和盲人用户有用。
.TP
.BR \-h ", " \-\-help
显示可用命令行选项的摘要并退出。
.TP
.BR \-i ", " \-\-autoindent
自动缩进新创建的行，使其与前一行的制表符和/或空格数量相同
（或者如果前一行是段落的开头，则与下一行相同）。
.TP
.BR \-j ", " \-\-jumpyscrolling
按半屏而不是按行滚动缓冲区内容。
.TP
.BR \-k ", " \-\-cutfromcursor
使"剪切文本"命令（通常为 \fB^K\fR）
从当前光标位置剪切到行尾，而不是剪切整行。
.TP
.BR \-l ", " \-\-linenumbers
在文本区域左侧显示行号。
（任何带有锚点的行还会在页边距中获得一个标记。）
.TP
.BR \-m ", " \-\-mouse
启用鼠标支持，如果您的系统支持的话。
启用后，鼠标点击可用于放置光标、
设置标记（通过双击）以及执行快捷键。
鼠标在 X Window 系统中工作，
在 gpm 运行时在控制台上也工作。
仍可通过按住 Shift 键拖动来选择文本。
.TP
.BR \-n ", " \-\-noread
将命令行上给出的任何名称视为新文件。
这允许 \fBnano\fR 写入命名管道：
它从一个空白缓冲区开始，当用户保存该"文件"时写入管道。
这样 \fBnano\fR 可以结合例如 \fBgpg\fR 作为编辑器使用，
而无需先将敏感数据写入磁盘。
.TP
.BI \-o " 目录\fR, " \-\-operatingdir=目录
切换到给定的\fI\%目录\fR，
并仅允许在此目录及其子目录中读写文件。
.TP
.BR \-p ", " \-\-preserve
保留 XOFF 和 XON 序列（\fB^S\fR 和 \fB^Q\fR），
以便它们被终端捕获（停止和恢复输出）。
注意选项 \fB\-/\fR（\fB\-\-\%modernbindings\fR）会覆盖此设置。
.TP
.BR \-q ", " \-\-indicator
在编辑窗口右侧显示一个"滚动条"。
它显示视口在缓冲区中的位置
以及缓冲区被视口覆盖了多少。
.TP
.BI \-r " 数字\fR, " \-\-fill=数字
设置在此 \fI\%数字\fR 列处进行对齐和自动硬折行的目标宽度。
如果该值小于或等于 0，则在屏幕宽度减去 \fI\%数字\fR 列处进行折行，
允许折行点随屏幕宽度变化（如果屏幕大小被调整）。
默认值为 \fB\-8\fR。
.TP
.B "\-s ""\fI程序\fR [\fI参数\fR ...]\fB""\fR, \fB\-\-speller=""\fI程序\fR [\fI参数\fR ...]\fB"""
使用此命令执行拼写检查和纠正，
而不是使用内置的调用 \fB\%hunspell\fR(1) 或 \fBspell\fR(1) 的纠正器。
.TP
.BR \-t ", " \-\-saveonexit
保存更改的缓冲区而不提示（当使用 \fB^X\fR 退出时）。
.TP
.BR \-u ", " \-\-unix
默认以 Unix 格式保存文件。
这会覆盖 \fBnano\fR 默认以文件原有格式保存的行为。
（当您同时使用 \fB\-\-\%noconvert\fR 时，此选项无效。）
.TP
.BR \-v ", " \-\-view
仅查看文件并禁止编辑：只读模式。
此模式允许用户也打开其他文件进行查看，
除非同时给出了 \fB\-\-\%restricted\fR。
.TP
.BR \-w ", " \-\-nowrap
当前行变得过长时不要自动硬折行。这是默认行为。
（此选项与 \fB\-b\fR（\fB\-\-\%breaklonglines\fR）相反——
最后给定的一个生效。）
.TP
.BR \-x ", " \-\-nohelp
不显示屏幕底部的两行帮助信息。
.TP
.BR \-y ", " \-\-afterends
使 Ctrl+Right 和 Ctrl+Delete 停在单词结尾而不是单词开头。
.TP
.BR \-z ", " \-\-listsyntaxes
列出可用语法名称并退出。
.TP
.BR \-! ", " \-\-magic
当文件名及其第一行都没有提供线索时，
尝试使用 \fI\%libmagic\fR 来确定适用的语法。
.TP
.BR \-@ ", " \-\-colonparsing
当命令行上给出的文件名以冒号加数字结尾
且此文件名不存在时，则截去冒号加数字
并将这些数字理解为行号。
如果修剪后的文件名也不存在，则重复此过程
并将得到的两个数字理解为行号和列号。
但如果两次修剪后的文件名也不存在，
则放弃修剪并接受原始文件名。
要为某些文件禁用此冒号解析，
请在相关文件名前使用 \fB+1\fR 或类似方式。
.TP
.BR \-% ", " \-\-stateflags
使用屏幕右上角显示一些状态标志：
\fBI\fR 表示自动缩进，\fBM\fR 表示标记已开启，
\fBL\fR 表示硬折行（断长行），\fBR\fR 表示正在录制宏，
\fBS\fR 表示软折行。
当缓冲区被修改时，在标题栏中央的文件名后会显示一个星号（\fB*\fR）。
.TP
.BR \-_ ", " \-\-minibar
隐藏标题栏，而是在屏幕底部状态栏的空间中显示当前缓冲区信息。
在这个"迷你栏"中，左侧显示文件名，
如果缓冲区已被修改则后跟一个星号。
右侧显示当前行号和列号、光标下字符的编码（Unicode 格式：U+xxxx）、
与 \fB\-\-\%stateflags\fR 显示相同的标志，
以及表示光标在文件中位置的百分比（按行计算）。
当文件被加载或保存时，以及在缓冲区之间切换时，
缓冲区中的行数会在文件名后显示。
此数字会在下一次按键时清除，
或者当多个缓冲区打开时替换为 [i/n] 计数器。
行号、列号和字符编码仅在同时使用
\fB\-\-\%constantshow\fR 时显示，
并且可以通过 \fBM\-C\fR 切换开和关。
状态标志仅在同时使用 \fB\-\-\%stateflags\fR 时显示。
.TP
.BR \-0 ", " \-\-zero
隐藏界面的所有元素（标题栏、状态栏和帮助行）
并使用终端的所有行来显示缓冲区内容。
状态栏仅在存在重要消息时出现，
并在 1.5 秒后或下一次按键时消失。
使用 \fBM\-Z\fR 可以切换标题栏加状态栏。
使用 \fBM\-X\fR 可以切换帮助行。
.TP
.BR \-1 ", " \-\-solosidescroll
当光标接近编辑窗口的右边缘时，
仅将当前行大幅度侧向滚动，
而不是将所有可见行侧向滚动刚好所需的最小量以保持光标在视野内。
后者（全窗口平滑侧向滚动）是新的默认行为。
此选项启用的单行跳跃式侧向滚动是旧行为——
也是 \fBnano\fR 曾经知道的唯一行为。
.TP
.BR \-/ ", " \-\-modernbindings
使用与大多数现代程序类似的按键绑定：
\fB^X\fR 剪切，\fB^C\fR 复制，\fB^V\fR 粘贴，
\fB^Z\fR 撤销，\fB^Y\fR 重做，
\fB^F\fR 向前搜索，\fB^G\fR 搜索下一个，
\fB^S\fR 保存，\fB^O\fR 打开文件，
\fB^Q\fR 退出，
以及（当终端允许时）\fB^H\fR 显示帮助。
此外，\fB^A\fR 设置标记，
\fB^B\fR 向后搜索，
\fB^D\fR 搜索上一个，
\fB^R\fR 进行替换，
\fB^P\fR 显示位置，
\fB^T\fR 转到某行，
\fB^W\fR 写出文件，
以及 \fB^E\fR 执行命令。
注意此选项会覆盖选项 \fB\-p\fR（\fB\-\-\%preserve\fR）。

.SH 切换选项
上述多个选项也可以在 \fBnano\fR 运行时切换开和关。
例如，\fBM\-L\fR 切换长行硬折行，\fBM\-S\fR 切换软折行，
\fBM\-N\fR 切换行号，\fBM\-M\fR 切换鼠标，
\fBM\-I\fR 切换自动缩进，\fBM\-X\fR 切换帮助行。
请参阅 \fB^G\fR 帮助文本末尾的完整列表。
.sp
\fBM\-X\fR 切换很特殊：它在除帮助查看器和语法检查器之外的所有菜单中均有效。
所有其他切换仅在主菜单中有效。

.SH 文件
当给出 \fB\-\-\%rcfile\fR 时，\fBnano\fR 仅读取指定的文件
来设置其选项、语法和按键绑定。
没有此选项时，\fBnano\fR 读取两个配置文件：
首先是系统的 \fInanorc\fR（如果存在），
然后是用户的 \fInanorc\fR（如果存在），
依次查找 \fI\%\[ti]/.nanorc\fR 或 \fI\%$XDG_CONFIG_HOME/\:nano/\:nanorc\fR
或 \fI\%\[ti]/.config/\:nano/\:nanorc\fR，以最先找到的为准。
请参阅
.BR nanorc (5)
了解这些文件可能内容的更多信息。
.sp
请参阅 \fI/usr/\:share/\:nano/\fR 和 \fI/usr/\:share/\:nano/\:extra/\fR
获取可用的语法着色定义。

.SH 退出状态
.TP
.B 0
编辑器正常结束。
.TP
.B 1
给出了无效的选项或选项参数。
.TP
.B 2
最后一个打开的缓冲区被丢弃（使用 \fB^O^Q\fR 或 \fB^X^Q\fR）。

.SH 备注
挂起功能默认启用，通过 \fB^T^Z\fR 访问。
（如果您希望普通的 \fB^Z\fR 挂起 nano，
请在 \fInanorc\fR 中添加 \fBbind ^Z suspend main\fR。）
.sp
在 Yes-No 提示符下，\fB^Y\fR 可用于"是"，
\fB^N\fR 可用于"否"，
\fB^A\fR 可用于"全部"。
（退出时，\fB^Q\fR 可用于"带错误退出"。）
这些未列出的绑定在任何语言环境中都有效。
.sp
当您想要将 \fBnano\fR 中标记的文本复制到系统剪贴板时，
请参阅 \fBnanorc\fR(5) 手册页中的示例之一。
.sp
如果在命令行或任何 \fInanorc\fR 文件中未指定替代拼写检查器命令，
\fBnano\fR 会检查 \fBSPELL\fR 环境变量以获取一个。
.sp
在某些情况下，\fBnano\fR 会尝试将缓冲区转储到紧急文件中。
这主要发生在 \fBnano\fR 收到 SIGHUP 或 SIGTERM 信号，
或内存不足时。
它会将缓冲区写入名为 \fI\%nano.save\fR 的文件
（如果缓冲区尚未有名称），
或者将 ".save" 后缀添加到当前文件名。
如果当前目录中已存在同名的紧急文件，
它会将 ".save" 加上一个数字（例如：".save.1"）
添加到当前文件名以使其唯一。
在多缓冲区模式下，
\fBnano\fR 将所有打开的缓冲区写入各自的紧急文件。
.sp
如果您对在特定情况下如何使用 \fBnano\fR 有任何疑问，
可以向 \fIhelp\-nano@gnu.org\fR 提问。

.SH 错误
键盘宏的录制和重放仅在终端模拟器上正确工作，
在 Linux 控制台（VT）上不能，
因为后者默认不区分已修改和未修改的方向键。
.sp
请通过以下网址报告您遇到的任何其他错误：
.br
.IR https://savannah.gnu.org/bugs/?group=nano 。
.sp
当 \fBnano\fR 崩溃时，它会将任何修改过的缓冲区
保存到紧急的 \fI.save\fR 文件中。
如果您能够重现该崩溃并希望获取回溯信息，
请定义环境变量 \fBNANO_NOCATCH\fR。

.SH 主页
.I https://nano\-editor.org/

.SH 参见
.BR nanorc (5)
.sp
\fI/usr/share/doc/nano/\fR（或您系统上的等效路径）

.SH "[中文版维护人]"
.B Aurolystant
.SH "[中文版最新更新]"
.BR 2026.07.13
.SH "[中国 Linux 论坛中文手册页翻译计划]"
.BI https://github.com/man-pages-zh/manpages-zh
