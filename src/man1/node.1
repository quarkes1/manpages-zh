.\"
.\" This file was generated automatically by the @node-core/doc-kit tool.
.\" Please do not edit this file manually. Make any updates to cli.md
.\" and regenerate the file afterward.
.\"
.\" To regenerate this file, run `make doc/node.1`.
.\"
.\"======================================================================
.Dd $Mdocdate$
.Dt NODE 1
.
.Sh 名称
.Nm node
.Nd 服务器端 JavaScript 运行时
.
.Sh 总览
.Nm node
.Op Ar 选项
.Op Ar v8 选项
.Op Ar <程序入口点> | Fl e Ar 字符串 | Fl -
.Op Ar 参数 ...
.
.Nm node
.Cm inspect,
.Op Ar <程序入口点> | Fl e Ar 字符串 | Ar <主机>:<端口>
.Ar ...
.
.Nm node
.Op Fl -v8-options
.
.Sh 描述
Node.js 是一组用于 JavaScript 的库，使其能够在浏览器之外使用。

它主要专注于创建简单、易于构建的网络客户端和服务器。
.Pp
不带参数执行
.Nm
可启动一个 REPL（交互式解释器）。
.
.Sh 选项
.Bl -tag -width 6n
.It Fl
stdin 的别名。类似于其他命令行工具中使用 \fB-\fR 的方式，
表示从 stdin 读取脚本，
其余选项传递给该脚本。
.
.It Fl -
表示 node 选项的结束。将剩余参数传递给脚本。
如果在此之前没有提供脚本文件名或 eval/print 脚本，
则
下一个参数将被用作脚本文件名。
.
.It Fl -abort-on-uncaught-exception
在发生未捕获异常时中止而非退出，
这将生成一个核心文件，
用于使用调试器（如 \fBlldb\fR、\fBgdb\fR 和 \fBmdb\fR）进行事后分析。

如果传递了此标志，仍可以通过
\fBprocess.setUncaughtExceptionCaptureCallback()\fR（以及使用
利用了该函数的 \fBnode:domain\fR 模块）将行为设置为不中止。
.
.It Fl -allow-addons
使用权限模型时，
进程默认无法使用原生插件。
除非用户在启动 Node.js 时显式传递 \fB--allow-addons\fR 标志，
否则尝试这样做将抛出 \fBERR_DLOPEN_DISABLED\fR 错误。
示例：
.Bd -literal
// 尝试加载一个原生插件
require('nodejs-addon-example');
.Ed
.Bd -literal
$ node --permission --allow-fs-read=* index.js
node:internal/modules/cjs/loader:1319
  return process.dlopen(module, path.toNamespacedPath(filename));
                 ^

Error: Cannot load native addon because loading addons is disabled.
    at Module._extensions..node (node:internal/modules/cjs/loader:1319:18)
    at Module.load (node:internal/modules/cjs/loader:1091:32)
    at Module._load (node:internal/modules/cjs/loader:938:12)
    at Module.require (node:internal/modules/cjs/loader:1115:19)
    at require (node:internal/modules/helpers:130:18)
    at Object.<anonymous> (/home/index.js:1:15)
    at Module._compile (node:internal/modules/cjs/loader:1233:14)
    at Module._extensions..js (node:internal/modules/cjs/loader:1287:10)
    at Module.load (node:internal/modules/cjs/loader:1091:32)
    at Module._load (node:internal/modules/cjs/loader:938:12) {
  code: 'ERR_DLOPEN_DISABLED'
}
.Ed
.
.It Fl -allow-child-process
使用权限模型时，
进程默认无法生成任何子进程。
除非用户在启动 Node.js 时显式传递 \fB--allow-child-process\fR 标志，
否则尝试这样做将抛出 \fBERR_ACCESS_DENIED\fR 错误。
示例：
.Bd -literal
const childProcess = require('node:child_process');
// 尝试绕过权限
childProcess.spawn('node', [' -e', 'require("fs").writeFileSync("/new-file", "example")']);
.Ed
.Bd -literal
$ node --permission --allow-fs-read=* index.js
node:internal/child_process:388
  const err = this._handle.spawn(options);
                           ^
Error: Access to this API has been restricted
    at ChildProcess.spawn (node:internal/child_process:388:28)
    at node:internal/main/run_main_module:17:47 {
  code: 'ERR_ACCESS_DENIED',
  permission: 'ChildProcess'
}
.Ed
\fBchild_process.fork()\fR API 会继承父进程的执行参数。

这意味着如果 Node.js 在启用权限模型的情况下启动，
并且设置了
\fB--allow-child-process\fR 标志，则使用 \fBchild_process.fork()\fR 创建的任何子进程
将自动接收所有相关的权限模型标志。
此行为也适用于 \fBchild_process.spawn()\fR，但在这种情况下，
标志是通过 \fBNODE_OPTIONS\fR 环境变量传播的，而非直接通过进程参数。

.
.It Fl -allow-ffi
使用权限模型时，
进程默认无法使用 FFI API。
除非用户在启动 Node.js 时显式传递 \fB--allow-ffi\fR 标志，
否则尝试使用 FFI API 将抛出 \fBERR_ACCESS_DENIED\fR 异常。

\fBnode:ffi\fR 模块还需要 \fB--experimental-ffi\fR 标志，并且仅在有 FFI 支持的构建中可用。

示例：
.Bd -literal
const { DynamicLibrary } = require('node:ffi');
const lib = new DynamicLibrary('mylib.so');
.Ed
.Bd -literal
$ node --permission --experimental-ffi index.js
Error: Access to this API has been restricted. Use --allow-ffi to manage permissions.
    at node:internal/main/run_main_module:17:47 {
  code: 'ERR_ACCESS_DENIED',
  permission: 'FFI'
}
.Ed
.
.It Fl -allow-fs-read
此标志使用权限模型配置文件系统读取权限。
\fB--allow-fs-read\fR 标志的有效参数有：
.Bl -bullet
.It
\fB*\fR - 允许所有 \fBFileSystemRead\fR 操作。
.It
可以使用多个 \fB--allow-fs-read\fR 标志允许多个路径。
示例 \fB--allow-fs-read=/folder1/ --allow-fs-read=/folder1/\fR
.El
示例可在文件系统权限文档中找到。
初始化模块和自定义 \fB--require\fR 模块具有隐式的读取权限。
.Bd -literal
$ node --permission -r custom-require.js -r custom-require-2.js index.js
.Ed
.Bl -bullet
.It
\fBcustom-require.js\fR、\fBcustom-require-2.js\fR 和 \fBindex.js\fR 默认会
被添加到允许读取列表中。
.El
.Bd -literal
process.permission.has('fs.read', 'index.js'); // true
process.permission.has('fs.read', 'custom-require.js'); // true
process.permission.has('fs.read', 'custom-require-2.js'); // true
.Ed
.
.It Fl -allow-fs-write
此标志使用权限模型配置文件系统写入权限。
\fB--allow-fs-write\fR 标志的有效参数有：
.Bl -bullet
.It
\fB*\fR - 允许所有 \fBFileSystemWrite\fR 操作。
.It
可以使用多个 \fB--allow-fs-write\fR 标志允许多个路径。
示例 \fB--allow-fs-write=/folder1/ --allow-fs-write=/folder1/\fR
.El
不再允许使用逗号 (\fB,\fR) 分隔路径。
当使用包含逗号的单个标志时，
将显示一条警告。
示例可在文件系统权限文档中找到。
.
.It Fl -allow-inspector
使用权限模型时，
进程默认无法通过检查器协议进行连接。
除非用户在启动 Node.js 时显式传递 \fB--allow-inspector\fR 标志，
否则尝试这样做将抛出 \fBERR_ACCESS_DENIED\fR 错误。
示例：
.Bd -literal
const { Session } = require('node:inspector/promises');

const session = new Session();
session.connect();
.Ed
.Bd -literal
$ node --permission index.js
Error: connect ERR_ACCESS_DENIED Access to this API has been restricted. Use --allow-inspector to manage permissions.
  code: 'ERR_ACCESS_DENIED',
}
.Ed
.
.It Fl -allow-net
使用权限模型时，进程默认无法访问网络。
除非用户在启动 Node.js 时显式传递 \fB--allow-net\fR 标志，
否则尝试这样做将抛出 \fBERR_ACCESS_DENIED\fR 错误。
示例：
.Bd -literal
const http = require('node:http');
// 尝试绕过权限
const req = http.get('http://example.com', () => {});

req.on('error', (err) => {
  console.log('err', err);
});
.Ed
.Bd -literal
$ node --permission index.js
Error: connect ERR_ACCESS_DENIED Access to this API has been restricted. Use --allow-net to manage permissions.
  code: 'ERR_ACCESS_DENIED',
}
.Ed
.
.It Fl -allow-wasi
使用权限模型时，
进程默认无法创建任何 WASI 实例。
出于安全原因，
除非用户在主 Node.js 进程中显式传递
\fB--allow-wasi\fR 标志，否则该调用将抛出 \fBERR_ACCESS_DENIED\fR 错误。

示例：
.Bd -literal
const { WASI } = require('node:wasi');
// 尝试绕过权限
new WASI({
  version: 'preview1',
  // 尝试挂载整个文件系统
  preopens: {
    '/': '/',
  },
});
.Ed
.Bd -literal
$ node --permission --allow-fs-read=* index.js

Error: Access to this API has been restricted
    at node:internal/main/run_main_module:30:49 {
  code: 'ERR_ACCESS_DENIED',
  permission: 'WASI',
}
.Ed
.
.It Fl -allow-worker
使用权限模型时，
进程默认无法创建工作线程。
出于安全原因，
除非用户在主 Node.js 进程中显式传递
\fB--allow-worker\fR 标志，否则该调用将抛出 \fBERR_ACCESS_DENIED\fR 错误。

示例：
.Bd -literal
const { Worker } = require('node:worker_threads');
// 尝试绕过权限
new Worker(__filename);
.Ed
.Bd -literal
$ node --permission --allow-fs-read=* index.js

Error: Access to this API has been restricted
    at node:internal/main/run_main_module:17:47 {
  code: 'ERR_ACCESS_DENIED',
  permission: 'WorkerThreads'
}
.Ed
.
.It Fl -build-sea Ns = Ns Ar config
从 JSON 配置文件生成单一可执行应用程序。
参数必须是配置文件的路径。如果路径不是绝对路径，
则根据当前工作目录进行解析。
关于配置字段、跨平台说明和资源 API，
请参阅单一可执行应用程序文档。
.
.It Fl -build-snapshot
在进程退出时生成快照 blob 并写入磁盘，以后可以使用 \fB--snapshot-blob\fR 加载。

构建快照时，
如果未指定 \fB--snapshot-blob\fR，
生成的 blob 默认将写入当前工作目录中的 \fBsnapshot.blob\fR。

否则将写入 \fB--snapshot-blob\fR 指定的路径。
.Bd -literal
$ echo "globalThis.foo = 'I am from the snapshot'" > snapshot.js

# 运行 snapshot.js 以初始化应用程序，并将其状态快照到 snapshot.blob
$ node --snapshot-blob snapshot.blob --build-snapshot snapshot.js

$ echo "console.log(globalThis.foo)" > index.js

# 加载生成的快照并从 index.js 启动应用程序。
$ node --snapshot-blob snapshot.blob index.js
I am from the snapshot
.Ed
可以使用 \fBv8.startupSnapshot\fR API 在快照构建时指定入口点，
从而在反序列化时无需额外的入口脚本：
.Bd -literal
$ echo "require('v8').startupSnapshot.setDeserializeMainFunction(() => console.log('I am from the snapshot'))" > snapshot.js
$ node --snapshot-blob snapshot.blob --build-snapshot snapshot.js
$ node --snapshot-blob snapshot.blob
I am from the snapshot
.Ed
更多信息请查看 \fBv8.startupSnapshot\fR API 文档。
快照目前仅支持在快照构建过程中加载单个入口点，
可以加载内置模块，
但不能加载额外的用户层模块。
用户可以在构建快照之前使用自己选择的打包工具将应用程序打包到单个脚本中。
由于确保所有内置模块的可序列化性很复杂，
而且内置模块也在不断增长，
因此只有一部分内置模块经过了良好的测试，
可以在快照构建过程中进行序列化。
Node.js 核心测试套件会检查一些相当复杂的应用程序是否可以快照。
被 Node.js 内置快照捕获的内置模块列表被视为受支持。
当快照构建器遇到无法序列化的内置模块时，
可能会导致快照构建过程崩溃。在这种情况下，
典型的解决方法是延迟加载该模块到运行时，
使用 \fBv8.startupSnapshot.setDeserializeMainFunction()\fR 或
\fBv8.startupSnapshot.addDeserializeCallback()\fR。
如果在快照构建过程中需要对其他模块进行序列化，
请在 Node.js 问题跟踪器中提交请求，
并链接到用户层快照的跟踪问题。
.
.It Fl -build-snapshot-config
指定 JSON 配置文件的路径，
用于配置快照创建行为。
目前支持以下选项：
.Bl -bullet
.It
\fBbuilder\fR \fB<string>\fR 必需。
提供在构建快照之前执行的脚本名称，
就像传递了 \fB--build-snapshot\fR 并将 \fBbuilder\fR 作为主脚本名称一样。

.It
\fBwithoutCodeCache\fR \fB<boolean>\fR 可选。
包含代码缓存可以减少编译快照中包含的函数所需的时间，
但代价是快照体积更大，
并可能破坏快照的可移植性。
.El
使用此标志时，
命令行中提供的其他脚本文件将不会被执行，
而是被解释为常规命令行参数。
.
.It Fl c , Fl -check
语法检查脚本但不执行。
.
.It Fl -completion-bash
打印可用于 source 的 Node.js bash 补全脚本。
.Bd -literal
node --completion-bash > node_bash_completion
source node_bash_completion
.Ed
.
.It Fl C Ar condition , Fl -conditions Ns = Ns Ar condition
提供自定义的条件导出（conditional exports）解析条件。
允许任意数量的自定义字符串条件名称。
Node.js 的默认条件 \fB"node"\fR、\fB"default"\fR、\fB"import"\fR 和
\fB"require"\fR 将始终按定义应用。
例如，
要使用 "development" 解析方式运行模块：
.Bd -literal
node -C development app.js
.Ed
.
.It Fl -cpu-prof
在启动时启动 V8 CPU 分析器，
并在退出前将 CPU 分析结果写入磁盘。
如果未指定 \fB--cpu-prof-dir\fR，生成的分析结果将放置在当前工作目录中。

如果未指定 \fB--cpu-prof-name\fR，
生成的分析结果将
命名为 \fBCPU.${yyyymmdd}.${hhmmss}.${pid}.${tid}.${seq}.cpuprofile\fR。

.Bd -literal
$ node --cpu-prof index.js
$ ls *.cpuprofile
CPU.20190409.202950.15293.0.0.cpuprofile
.Ed
如果指定了 \fB--cpu-prof-name\fR，
提供的值将用作文件名的模板。
支持以下占位符，将在运行时被替换：
.Bl -bullet
.It
\fB${pid}\fR — 当前进程 ID
.El
.Bd -literal
$ node --cpu-prof --cpu-prof-name 'CPU.${pid}.cpuprofile' index.js
$ ls *.cpuprofile
CPU.15293.cpuprofile
.Ed
.
.It Fl -cpu-prof-dir
指定由 \fB--cpu-prof\fR 生成的 CPU 分析结果存放的目录。
默认值由 \fB--diagnostic-dir\fR 命令行选项控制。
.
.It Fl -cpu-prof-interval
指定由 \fB--cpu-prof\fR 生成的 CPU 分析结果的采样间隔（微秒）。
默认值为 1000 微秒。

.
.It Fl -cpu-prof-name
指定由 \fB--cpu-prof\fR 生成的 CPU 分析结果的文件名。
.
.It Fl -diagnostic-dir Ns = Ns Ar directory
设置所有诊断输出文件的写入目录。默认为当前工作目录。
影响以下选项的默认输出目录：
.Bl -bullet
.It
\fB--cpu-prof-dir\fR
.It
\fB--heap-prof-dir\fR
.It
\fB--redirect-warnings\fR
.El
.
.It Fl -disable-proto Ns = Ns Ar mode
禁用 \fBObject.prototype.__proto__\fR 属性。
如果 \fBmode\fR 为 \fBdelete\fR，
则完全移除该属性。如果 \fBmode\fR 为 \fBthrow\fR，
访问该属性将抛出代码为 \fBERR_PROTO_ACCESS\fR 的异常。
.
.It Fl -disable-sigusr1
禁用通过向进程发送 \fBSIGUSR1\fR 信号来启动调试会话的能力。
.
.It Fl -disable-warning Ns = Ns Ar code-or-type
按 \fBcode\fR 或 \fBtype\fR 禁用特定的进程警告。
从 \fBprocess.emitWarning()\fR 发出的警告可能包含
\fBcode\fR 和 \fBtype\fR。
此选项将禁止发出匹配 \fBcode\fR 或 \fBtype\fR 的警告。

弃用警告列表。
Node.js 核心警告类型为：\fBDeprecationWarning\fR 和
\fBExperimentalWarning\fR
例如，以下脚本在使用 \fBnode --disable-warning=DEP0025\fR 执行时不会发出
DEP0025 \fBrequire('node:sys')\fR：
.Bd -literal
import sys from 'node:sys';
.Ed
.Bd -literal
const sys = require('node:sys');
.Ed
例如，以下脚本在使用 \fBnode --disable-warning=ExperimentalWarning\fR 执行时
会发出 DEP0025 \fBrequire('node:sys')\fR，但不会发出任何 Experimental Warning
（例如在 <=v21 中 ExperimentalWarning: \fBvm.measureMemory\fR is an experimental feature）：
.Bd -literal
import sys from 'node:sys';
import vm from 'node:vm';

vm.measureMemory();
.Ed
.Bd -literal
const sys = require('node:sys');
const vm = require('node:vm');

vm.measureMemory();
.Ed
.
.It Fl -disable-wasm-trap-handler
Node.js 在 64 位平台上启用基于 V8 陷阱处理器的 WebAssembly 边界检查，
这通过消除内联边界检查的需求来显著提高 WebAssembly 性能。
此优化需要为每个 WebAssembly 内存实例分配一个大的虚拟内存笼子
（目前 32 位 WebAssembly 内存通常为 8GB，64 位 WebAssembly 内存为 16GB）
来捕获越界访问。
在大多数 64 位平台上，虚拟内存地址空间通常足够大（约 128TB）
来容纳典型的 WebAssembly 使用场景，但如果机器对虚拟内存设置了手动限制
（例如通过 \fBulimit -v\fR），WebAssembly 内存分配更可能失败，
并显示 \fBWebAssembly.Memory(): could not allocate memory\fR。

在启动时，
Node.js 会自动检查是否有足够的虚拟内存来分配至少一个笼子，
如果没有，陷阱处理器优化会自动禁用，以便 WebAssembly 仍然可以使用内联边界检查运行
（但性能较差）。但如果应用程序需要创建大量 WebAssembly 内存实例，
并且机器仍然配置了相对较高的虚拟内存限制，
由于虚拟内存使用量增加，
WebAssembly 内存实例的分配可能仍会比预期更快失败。
B--disable-wasm-trap-handler\fR 完全禁用此优化，
以便 WebAssembly 内存实例始终使用内联边界检查，而不是预留大的虚拟内存笼子。

这允许在 Node.js 进程可用的虚拟内存地址空间有限时创建更多实例。
.
.It Fl -disallow-code-generation-from-strings
使诸如 \fBeval\fR 和 \fBnew Function\fR 之类的内置语言特性
在从字符串生成代码时抛出异常。
这不影响 Node.js 的 \fBnode:vm\fR 模块。

.
.It Fl -dns-result-order Ns = Ns Ar order
设置 \fBdns.lookup()\fR 和 \fBdnsPromises.lookup()\fR
中 \fBorder\fR 的默认值。取值可以是：
.Bl -bullet
.It
\fBipv4first\fR：将默认 \fBorder\fR 设置为 \fBipv4first\fR。

.It
\fBipv6first\fR：将默认 \fBorder\fR 设置为 \fBipv6first\fR。

.It
\fBverbatim\fR：将默认 \fBorder\fR 设置为 \fBverbatim\fR。

.El
默认值为 \fBverbatim\fR，并且 \fBdns.setDefaultResultOrder()\fR 的优先级
高于 \fB--dns-result-order\fR。
.
.It Fl -enable-fips
在启动时启用符合 FIPS 标准的加密。
（需要 Node.js 针对兼容 FIPS 的 OpenSSL 构建。
）
.
.It Fl -enable-source-maps
为堆栈跟踪启用 Source Map 支持。
使用转译器（如 TypeScript）时，应用程序抛出的堆栈跟踪引用的是转译后的代码，而不是原始源代码位置。

\fB--enable-source-maps\fR 启用 Source Map 的缓存，并尽力报告相对于原始源文件的堆栈跟踪。

重写 \fBError.prepareStackTrace\fR 可能会阻止 \fB--enable-source-maps\fR 修改堆栈跟踪。

在重写函数中调用并返回原始 \fBError.prepareStackTrace\fR 的结果，以使用 source map 修改堆栈跟踪。

.Bd -literal
const originalPrepareStackTrace = Error.prepareStackTrace;
Error.prepareStackTrace = (error, trace) => {
  // 修改 error 和 trace，并使用原始 Error.prepareStackTrace 格式化堆栈跟踪
  return originalPrepareStackTrace(error, trace);
};
.Ed
注意，启用 source map 可能会在访问 \fBError.stack\fR 时给应用程序增加延迟。

如果您的应用程序频繁访问 \fBError.stack\fR，请考虑 \fB--enable-source-maps\fR 对性能的影响。

.
.It Fl -entry-url
如果设置，
Node.js 会将入口点解释为 URL 而不是路径。
遵循 ECMAScript 模块解析规则。
URL 中的任何查询参数或哈希值都可以通过 \fBimport.meta.url\fR 访问。

.Bd -literal
node --entry-url 'file:///path/to/file.js?queryparams=work#and-hashes-too'
node --entry-url 'file.ts?query#hash'
node --entry-url 'data:text/javascript,console.log("Hello")'
.Ed
.
.It Fl -env-file-if-exists Ns = Ns Ar file
行为与 \fB--env-file\fR 相同，
但如果文件不存在不会抛出错误。
.
.It Fl -env-file Ns = Ns Ar file
从相对于当前目录的文件加载环境变量，
使其通过 \fBprocess.env\fR 可供应用程序使用。
配置 Node.js 的环境变量，
如 \fBNODE_OPTIONS\fR，
也会被解析和应用。如果同一变量同时在环境和文件中定义，
环境中的值优先。
可以传递多个 \fB--env-file\fR 参数。
后续文件会覆盖之前文件中已存在的变量。

如果文件不存在，将抛出错误。
.Bd -literal
node --env-file=.env --env-file=.development.env index.js
.Ed
文件的格式应为每行一个环境变量键值对，
用 \fB=\fR 分隔：
.Bd -literal
PORT=3000
.Ed
\fB#\fR 之后的任何文本都被视为注释：
.Bd -literal
# 这是一个注释
PORT=3000 # 这也是注释
.Ed
值可以以以下引号开头和结尾：\fB`\fR、\fB"\fR 或 \fB'\fR。
这些引号会从值中移除。
.Bd -literal
USERNAME="nodejs" # 结果为 `nodejs` 作为值。
.Ed
支持多行值：
.Bd -literal
MULTI_LINE="THIS IS
A MULTILINE"
# 结果为 `THIS IS\nA MULTILINE` 作为值。
.Ed
键之前的 Export 关键字被忽略：
.Bd -literal
USERNAME="nodejs" # 结果为 `nodejs` 作为值。
.Ed
如果希望从可能不存在的文件加载环境变量，可以使用 \fB--env-file-if-exists\fR 标志。

.
.It Fl e , Fl -eval Ar "script"
将以下参数作为 JavaScript 求值。
REPL 中预定义的模块也可以在 \fBscript\fR 中使用。

如果 \fBscript\fR 以 \fB-\fR 开头，请使用 \fB=\fR 传递它（例如，
\fBnode --print --eval=-42\fR），以便将其解析为 \fB--eval\fR 的值。

在 Windows 上，使用 \fBcmd.exe\fR 时单引号不能正常工作，因为它只识别双引号 \fB"\fR。

在 Powershell 或 Git bash 中，\fB'\fR 和 \fB"\fR 都可以使用。

除非提供了 \fB--no-strip-types\fR 标志，否则可以运行包含内联类型的代码。

.
.It Fl -experimental-addon-modules
为 \fB.node\fR 插件启用实验性 import 支持。
.
.It Fl -experimental-config-file Ns = Ns Ar path , Fl -experimental-config-file
如果设置，
Node.js 将在指定路径查找配置文件。
如果未指定路径，Node.js 将在当前工作目录中查找 \fBnode.config.json\fR 文件。

要指定自定义路径，请使用 \fB--experimental-config-file=path\fR 形式。

不支持空格分隔的 \fB--experimental-config-file path\fR 形式。

别名 \fB--experimental-default-config-file\fR 相当于
不带参数的 \fB--experimental-config-file\fR。
Node.js 将读取配置文件并应用其中的设置。
配置文件应为 JSON 格式，具有以下结构。
\fB$schema\fR 中的 \fBvX.Y.Z\fR
必须替换为您使用的 Node.js 版本，或使用 \fBlatest-vX.x\fR 表示该主版本线的最新版本。

.Bd -literal
{
  "$schema": "https://nodejs.org/dist/vX.Y.Z/docs/node-config-schema.json",
  "nodeOptions": {
    "import": [
      "amaro/strip"
    ],
    "watch-path": "src",
    "watch-preserve-output": true
  },
  "test": {
    "test-isolation": "process"
  },
  "watch": {
    "watch-preserve-output": true
  }
}
.Ed
配置文件支持命名空间特定的选项：
.Bl -bullet
.It
\fBnodeOptions\fR 字段包含 \fBNODE_OPTIONS\fR 中允许的 CLI 标志。

.It
\fBtest\fR、\fBwatch\fR 和 \fBpermission\fR 等命名空间字段包含该子系统特定的配置。

.El
配置文件可以通过 \fBnodeVersion\fR 针对特定的 Node.js 主版本：
.Bd -literal
{
  "nodeVersion": 25,
  "nodeOptions": {
    "watch-path": "src"
  }
}
.Ed
要在同一文件中保留多个特定版本的配置，
请使用
\fBconfigs\fR 数组。
Node.js 将使用第一个 \fBnodeVersion\fR 与当前 Node.js 主版本匹配的条目：
.Bd -literal
{
  "$schema": "https://nodejs.org/dist/latest-v26.x/docs/node-config-schema.json",
  "configs": [
    {
      "nodeVersion": 25,
      "config": {
        "$schema": "https://nodejs.org/dist/latest-v25.x/docs/node-config-schema.json",
        "nodeOptions": {
          "watch-path": "src"
        }
      }
    }
  ]
}
.Ed
使用 \fBconfigs\fR 时，顶级只能包含 \fB$schema\fR 和 \fBconfigs\fR。

每个 \fBconfigs\fR 项必须定义一个整数 \fBnodeVersion\fR 和一个对象 \fBconfig\fR。

单个顶级配置不需要 \fBnodeVersion\fR，但如果存在，必须与当前 Node.js 主版本匹配。

当命名空间出现在配置文件中时，
Node.js 会自动启用对应的标志
（例如，\fB--test\fR、\fB--watch\fR、\fB--permission\fR）。

这允许您配置子系统特定选项，
而无需在命令行上显式传递标志。
例如：
.Bd -literal
{
  "test": {
    "test-isolation": "process"
  }
}
.Ed
等效于：
.Bd -literal
node --test --test-isolation=process
.Ed
要禁用自动标志同时仍使用命名空间选项，
可以在命名空间内显式
将标志设置为 \fBfalse\fR：
.Bd -literal
{
  "test": {
    "test": false,
    "test-isolation": "process"
  }
}
.Ed
不支持无操作标志。
目前并非所有 V8 标志都受支持。
可以使用官方 JSON schema 来验证配置文件，
该 schema 可能因 Node.js 版本而异。
配置文件中的每个键对应一个可以作为命令行参数传递的标志。
键的值是传递给该标志的值。
例如，
上面的配置文件等效于以下命令行参数：
.Bd -literal
node --import amaro/strip --watch-path=src --watch-preserve-output --test-isolation=process
.Ed
配置的优先级如下：
.Bl -bullet
.It
NODE_OPTIONS 和命令行选项
.It
Dotenv NODE_OPTIONS
.It
配置文件
.El
配置文件中的值不会覆盖环境变量、命令行选项或通过 \fB--env-file\fR 标志
解析的 \fBNODE_OPTIONS\fR 环境文件中的值。
同一命名空间内或不同命名空间之间的键不能重复。
如果配置文件中包含未知键或不能在命名空间中使用的键，
配置解析器将抛出错误。
Node.js 不会对用户提供的配置进行清理或验证，
因此\fB绝对不要\fR使用不受信任的配置文件。
.
.It Fl -experimental-default-config-file
此标志是不带参数的 \fB--experimental-config-file\fR 的别名。

如果设置，
Node.js 将在当前工作目录中查找
\fBnode.config.json\fR 文件并将其加载为配置文件。
.
.It Fl -experimental-eventsource
在全局作用域上启用 EventSource Web API 的暴露。
.
.It Fl -experimental-ffi
启用实验性的 \fBnode:ffi\fR 模块。
此标志仅在有 FFI 支持的构建中可用。
.
.It Fl -experimental-import-meta-resolve
启用实验性的 \fBimport.meta.resolve()\fR 父 URL 支持，允许
传递第二个 \fBparentURL\fR 参数进行上下文解析。
以前此标志控制整个 \fBimport.meta.resolve\fR 功能。
.
.It Fl -experimental-inspector-network-resource
启用检查器网络资源的实验性支持。
.
.It Fl -experimental-loader Ns = Ns Ar module
指定包含导出的异步模块自定义钩子的 \fBmodule\fR。
\fBmodule\fR 可以是任何被接受为 \fBimport\fR 说明符的字符串。
如果与权限模型一起使用，
此功能需要 \fB--allow-worker\fR。
.
.It Fl -experimental-network-inspection
启用 Chrome DevTools 网络检查的实验性支持。
.
.It Fl -experimental-package-map Ns = Ns Ar <path>
启用实验性的包映射解析。
\fBpath\fR 参数指定定义包解析映射的 JSON 配置文件的位置。

.Bd -literal
node --experimental-package-map=./package-map.json app.js
.Ed
启用后，
裸说明符解析会查阅包映射以进行解析。
这允许显式控制哪些包可以导入哪些依赖项。
有关配置文件格式和解析算法的详细信息，
请参阅包映射（Package maps）。
.
.It Fl -experimental-print-required-tla
如果要被 \fBrequire()\fR 加载的 ES 模块包含顶层 \fBawait\fR，
此标志允许 Node.js 求值该模块，
尝试定位顶层 await，
并打印它们的位置以帮助用户找到它们。
.
.It Fl -experimental-quic
启用 QUIC 协议的实验性支持。
.
.It Fl -experimental-sea-config
使用此标志生成一个 blob，可以注入到 Node.js 二进制文件中以产生单一可执行应用程序。

有关详细信息，请参阅此配置的文档。
.
.It Fl -experimental-shadow-realm
使用此标志启用 ShadowRealm 支持。
.
.It Fl -experimental-storage-inspection
启用存储检查的实验性支持。
.
.It Fl -experimental-stream-iter
启用实验性的 \fBnode:stream/iter\fR 模块。
.
.It Fl -experimental-test-coverage
当与 \fBnode:test\fR 模块一起使用时，将生成代码覆盖率报告作为测试运行器输出的一部分。

如果没有运行测试，则不会生成覆盖率报告。
有关详细信息，
请参阅从测试中收集代码覆盖率的文档。
.
.It Fl -experimental-test-module-mocks
在测试运行器中启用模块模拟。
如果与权限模型一起使用，
此功能需要 \fB--allow-worker\fR。
.
.It Fl -experimental-test-tag-filter Ns = Ns Ar <tag>
仅运行标签集包含 \fB<tag>\fR 的测试。
测试通过 \fBtest()\fR、\fBit()\fR、\fBsuite()\fR
或 \fBdescribe()\fR 上的 \fBtags\fR 选项声明标签；
标签通过并集从套件继承到嵌套测试。

过滤不区分大小写。
该标志可以指定多次；测试必须\fB同时\fB包含每个过滤值才能运行。
有关声明和继承标签的详细信息，
请参阅测试标签（Test tags）。
.
.It Fl -experimental-vfs
启用实验性的 \fBnode:vfs\fR 模块。
.
.It Fl -experimental-vm-modules
在 \fBnode:vm\fR 模块中启用实验性的 ES 模块支持。
.
.It Fl -experimental-wasi-unstable-preview1
启用实验性的 WebAssembly 系统接口（WASI）支持。
.
.It Fl -experimental-worker-inspection
启用 Chrome DevTools 工作线程检查的实验性支持。
.
.It Fl -expose-gc
此标志将暴露 V8 的 gc 扩展。
.Bd -literal
if (globalThis.gc) {
  globalThis.gc();
}
.Ed
.
.It Fl -force-context-aware
禁用加载非上下文感知的原生插件。
.
.It Fl -force-fips
在启动时强制使用符合 FIPS 标准的加密。（无法从脚本代码禁用。）
（与 \fB--enable-fips\fR 要求相同。）
.
.It Fl -force-node-api-uncaught-exceptions-policy
对 Node-API 异步回调强制执行 \fBuncaughtException\fR 事件。

为防止现有插件导致进程崩溃，
此标志默认未启用。
将来，
此标志将默认启用以强制执行正确行为。
.
.It Fl -frozen-intrinsics
启用实验性的冻结内置对象，
如 \fBArray\fR 和 \fBObject\fR。
仅支持根上下文。
不能保证 \fBglobalThis.Array\fR 确实是默认的内置引用。

代码在此标志下可能会出问题。
为了允许添加 polyfill，
\fB--require\fR 和 \fB--import\fR 在冻结内置对象之前运行。

.
.It Fl -heap-prof
在启动时启动 V8 堆分析器，
并在退出前将堆分析结果写入磁盘。
如果未指定 \fB--heap-prof-dir\fR，生成的分析结果将放置在当前工作目录中。

如果未指定 \fB--heap-prof-name\fR，
生成的分析结果将
命名为 \fBHeap.${yyyymmdd}.${hhmmss}.${pid}.${tid}.${seq}.heapprofile\fR。

.Bd -literal
$ node --heap-prof index.js
$ ls *.heapprofile
Heap.20190409.202950.15293.0.001.heapprofile
.Ed
.
.It Fl -heap-prof-dir
指定由 \fB--heap-prof\fR 生成的堆分析结果存放的目录。
默认值由 \fB--diagnostic-dir\fR 命令行选项控制。
.
.It Fl -heap-prof-interval
指定由 \fB--heap-prof\fR 生成的堆分析结果的平均采样间隔（字节）。
默认值为 512 * 1024 字节。

.
.It Fl -heap-prof-name
指定由 \fB--heap-prof\fR 生成的堆分析结果的文件名。
.
.It Fl -heapsnapshot-near-heap-limit Ns = Ns Ar max_count
当 V8 堆使用量接近堆限制时，
将 V8 堆快照写入磁盘。
\fBcount\fR 应为非负整数（在这种情况下 Node.js 最多写入 \fBmax_count\fR 个快照到磁盘）。

生成快照时，
可能会触发垃圾回收并降低堆使用量。
因此在 Node.js 实例最终耗尽内存之前，
可能会将多个快照写入磁盘。
这些堆快照可以进行比较，
以确定在连续拍摄快照期间分配了哪些对象。
不能保证 Node.js 恰好将 \fBmax_count\fR 个快照写入磁盘，
但当 \fBmax_count\fR 大于 \fB0\fR 时，它会尽力在 Node.js 实例耗尽内存之前
生成至少一个且最多 \fBmax_count\fR 个快照。
生成 V8 快照需要时间和内存（包括 V8 堆管理的内存和 V8 堆外的原生内存）。

堆越大，需要的资源越多。
Node.js 将调整 V8 堆以容纳额外的 V8 堆内存开销，
并尽力避免耗尽进程可用的所有内存。
当进程使用的内存超过系统认为适当的量时，
系统可能会根据系统配置突然终止进程。
.Bd -literal
$ node --max-old-space-size=100 --heapsnapshot-near-heap-limit=3 index.js
Wrote snapshot to Heap.20200430.100036.49580.0.001.heapsnapshot
Wrote snapshot to Heap.20200430.100037.49580.0.002.heapsnapshot
Wrote snapshot to Heap.20200430.100038.49580.0.003.heapsnapshot

<--- Last few GCs --->

[49580:0x110000000]     4826 ms: Mark-sweep 130.6 (147.8) -> 130.5 (147.8) MB, 27.4 / 0.0 ms  (average mu = 0.126, current mu = 0.034) allocation failure scavenge might not succeed
[49580:0x110000000]     4845 ms: Mark-sweep 130.6 (147.8) -> 130.6 (147.8) MB, 18.8 / 0.0 ms  (average mu = 0.088, current mu = 0.031) allocation failure scavenge might not succeed


<--- JS stacktrace --->

FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
....
.Ed
.
.It Fl -heapsnapshot-signal Ns = Ns Ar signal
启用信号处理器，
当收到指定信号时使 Node.js 进程写入堆转储。
\fBsignal\fR 必须是有效的信号名称。默认禁用。
.Bd -literal
$ node --heapsnapshot-signal=SIGUSR2 index.js &
$ ps aux
USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
node         1  5.5  6.1 787252 247004 ?       Ssl  16:43   0:02 node --heapsnapshot-signal=SIGUSR2 index.js
$ kill -USR2 1
$ ls
Heap.20190718.133405.15554.0.001.heapsnapshot
.Ed
.
.It Fl h , Fl -help
打印 node 命令行选项。
此选项的输出没有本文档详细。
.
.It Fl -icu-data-dir Ns = Ns Ar file
指定 ICU 数据加载路径。（覆盖 \fBNODE_ICU_DATA\fR。）
.
.It Fl -import Ns = Ns Ar module
在启动时预加载指定模块。如果多次提供此标志，
每个模块将按出现顺序依次执行，从 \fBNODE_OPTIONS\fR 中提供的模块开始。

遵循 ECMAScript 模块解析规则。
使用 \fB--require\fR 加载 CommonJS 模块。
通过 \fB--require\fR 预加载的模块将在通过 \fB--import\fR 预加载的模块之前运行。

模块将在主线程以及任何工作线程、子进程或集群进程中预加载。
.
.It Fl -input-type Ns = Ns Ar type
配置 Node.js 将 \fB--eval\fR 或 \fBSTDIN\fR 输入解释为 CommonJS 或 ES 模块。

有效值为 \fB"commonjs"\fR、\fB"module"\fR、\fB"module-typescript"\fR 和 \fB"commonjs-typescript"\fR。

如果传递了 \fB--no-strip-types\fR 标志，则 \fB"-typescript"\fR 值不可用。

默认值为空，或者如果传递了 \fB--no-experimental-detect-module\fR，则为 \fB"commonjs"\fR。

如果未提供 \fB--input-type\fR，Node.js 将尝试通过以下步骤检测语法：
.Bl -bullet
.It
将输入作为 CommonJS 运行。
.It
如果步骤 1 失败，
将输入作为 ES 模块运行。
.It
如果步骤 2 因 SyntaxError 失败，
剥离类型。
.It
如果步骤 3 因错误代码 \fBERR_UNSUPPORTED_TYPESCRIPT_SYNTAX\fR
或 \fBERR_INVALID_TYPESCRIPT_SYNTAX\fR 失败，
抛出步骤 2 的错误，
并在消息中包含 TypeScript 错误，
否则作为 CommonJS 运行。
.It
如果步骤 4 失败，
将输入作为 ES 模块运行。
.El
为避免多次语法检测传递带来的延迟，可以使用 \fB--input-type=type\fR 标志来指定
如何解释 \fB--eval\fR 输入。
REPL 不支持此选项。
将 \fB--input-type=module\fR 与 \fB--print\fR 一起使用将抛出错误，
因为 \fB--print\fR 不支持 ES 模块语法。
.
.It Fl -insecure-http-parser
在 HTTP 解析器上启用宽松标志。
这可能会允许与不符合标准的 HTTP 实现进行互操作。

启用后，解析器将接受以下内容：
.Bl -bullet
.It
无效的 HTTP 头值。
.It
无效的 HTTP 版本。
.It
允许消息同时包含 \fBTransfer-Encoding\fR 和 \fBContent-Length\fR 头。

.It
在 \fBConnection: close\fR 存在时允许消息后有额外数据。
.It
在已提供 \fBchunked\fR 后允许额外的传输编码。
.It
允许使用 \fB\n\fR 作为令牌分隔符而不是 \fB\r\n\fR。
.It
允许在数据块后不提供 \fB\r\n\fR。
.It
允许在数据块大小后和 \fB\r\n\fR 前出现空格。
.El
以上所有内容都会使您的应用程序暴露于请求走私或投毒攻击。避免使用此选项。
.
.It Fl -inspect-brk Ns = Ns Ar [[host:]port]
在 \fBhost:port\fR 上激活检查器，
并在用户脚本开始时中断。
默认 \fBhost:port\fR 为 \fB127.0.0.1:9229\fR。
如果指定端口 \fB0\fR，
将使用随机可用端口。
有关 Node.js 调试器的进一步说明，请参见 Node.js 的 V8 Inspector 集成。

请查看下面关于 \fBhost\fR 参数使用的安全警告。
.
.It Fl -inspect-port Ns = Ns Ar [host:]port
设置激活检查器时要使用的 \fBhost:port\fR。
在通过发送 \fBSIGUSR1\fR 信号激活检查器时很有用。
除非传递了 \fB--disable-sigusr1\fR。
默认主机为 \fB127.0.0.1\fR。如果指定端口 \fB0\fR，
将使用随机可用端口。
请查看下面关于 \fBhost\fR 参数使用的安全警告。
.
.It Fl -inspect-publish-uid Ns = Ns Ar stderr,http
指定检查器 WebSocket URL 的暴露方式。
默认情况下，检查器 WebSocket URL 在 stderr 中以及通过 \fBhttp://host:port/json/list\fR
端点上的 \fB/json/list\fR 可用。
.
.It Fl -inspect-wait Ns = Ns Ar [[host:]port]
在 \fBhost:port\fR 上激活检查器并等待调试器附加。
默认 \fBhost:port\fR 为 \fB127.0.0.1:9229\fR。
如果指定端口 \fB0\fR，
将使用随机可用端口。
有关 Node.js 调试器的进一步说明，请参见 Node.js 的 V8 Inspector 集成。

请查看下面关于 \fBhost\fR 参数使用的安全警告。
.
.It Fl -inspect Ns = Ns Ar [[host:]port]
在 \fBhost:port\fR 上激活检查器。
默认值为 \fB127.0.0.1:9229\fR。
如果指定端口 \fB0\fR，
将使用随机可用端口。
V8 检查器集成允许 Chrome DevTools 和 IDE 等工具调试和分析 Node.js 实例。

这些工具通过 tcp 端口附加到 Node.js 实例，并使用 Chrome DevTools 协议进行通信。

有关 Node.js 调试器的进一步说明，请参见 Node.js 的 V8 Inspector 集成。

.
.It Fl i , Fl -interactive
即使 stdin 看起来不是终端，
也打开 REPL。
.
.It Fl -jitless
禁用可执行内存的运行时分配。在某些平台上可能出于安全原因需要这样做。
它也可以减少其他平台上的攻击面，
但性能影响可能很严重。
.
.It Fl -localstorage-file Ns = Ns Ar file
用于存储 \fBlocalStorage\fR 数据的文件。如果文件不存在，
将在首次访问 \fBlocalStorage\fR 时创建。
同一文件可以在多个 Node.js 进程之间同时共享。

.
.It Fl -max-http-header-size Ns = Ns Ar size
指定 HTTP 头的最大大小（以字节为单位）。默认为 16 KiB。
.
.It Fl -max-old-space-size-percentage Ns = Ns Ar percentage
将 V8 旧内存区域的最大内存大小设置为可用系统内存的百分比。
当同时指定时，
此标志优先于 \fB--max-old-space-size\fR。
\fBpercentage\fR 参数必须是一个大于 0 且不超过 100 的数字，表示分配给 V8 堆的可用系统内存百分比。

\fB注意：\fR 此标志使用 \fB--max-old-space-size\fR，在 32 位平台上可能因整数溢出问题而不可靠。

.Bd -literal
# 使用 50% 的可用系统内存
node --max-old-space-size-percentage=50 index.js

# 使用 75% 的可用系统内存
node --max-old-space-size-percentage=75 index.js
.Ed
.
.It Fl -napi-modules
此选项为无操作选项。保留是为了兼容性。
.
.It Fl -network-family-autoselection-attempt-timeout
设置网络族自动选择尝试超时的默认值。
有关更多信息，请参见 \fBnet.getDefaultAutoSelectFamilyAttemptTimeout()\fR。

.
.It Fl -no-addons
禁用 \fBnode-addons\fR 导出条件并禁用加载原生插件。
当指定 \fB--no-addons\fR 时，调用 \fBprocess.dlopen\fR 或加载原生 C++ 插件将失败并抛出异常。

.
.It Fl -no-async-context-frame
禁用基于 \fBAsyncContextFrame\fR 的 \fBAsyncLocalStorage\fR，
改用依赖 async_hooks 的先前实现。
保留先前模型是为了与 Electron 兼容，
以及用于上下文流程可能不同的情况。
但如果发现流程差异，请报告。
.
.It Fl -no-deprecation
静默弃用警告。
.
.It Fl -no-experimental-detect-module
禁用使用语法检测来确定模块类型。
.
.It Fl -no-experimental-global-navigator
禁用全局作用域上 Navigator API 的暴露。
.
.It Fl -no-experimental-repl-await
使用此标志禁用 REPL 中的顶层 await。
.
.It Fl -no-experimental-require-module
\fB--no-require-module\fR 的旧别名。
.
.It Fl -no-experimental-sqlite
禁用实验性的 \fBnode:sqlite\fR 模块。
.
.It Fl -no-experimental-websocket
禁用全局作用域上 \fB<WebSocket>\fR 的暴露。
.
.It Fl -no-experimental-webstorage
禁用 Web Storage 支持。
.
.It Fl -no-extra-info-on-fatal-exception
隐藏导致退出的致命异常的额外信息。
.
.It Fl -no-force-async-hooks-checks
禁用 \fBasync_hooks\fR 的运行时检查。
当 \fBasync_hooks\fR 启用时，这些检查仍将动态启用。

.
.It Fl -no-global-search-paths
不要从 \fB$HOME/.node_modules\fR 和 \fB$NODE_PATH\fR 等全局路径搜索模块。

.
.It Fl -no-network-family-autoselection
禁用族自动选择算法，
除非连接选项显式启用它。
.
.It Fl -no-require-module
禁用在 \fBrequire()\fR 中加载同步 ES 模块图的支持。
请参阅使用 \fBrequire()\fR 加载 ECMAScript 模块。
.
.It Fl -no-strip-types
禁用 TypeScript 文件的类型剥离。
有关更多信息，
请参阅 TypeScript 类型剥离文档。
.
.It Fl -no-warnings
静默所有进程警告（包括弃用）。
.
.It Fl -node-memory-debug
启用 Node.js 内部内存泄漏的额外调试检查。
这通常只对调试 Node.js 本身的开发人员有用。
.
.It Fl -openssl-config Ns = Ns Ar file
在启动时加载 OpenSSL 配置文件。
除其他用途外，如果 Node.js 是针对启用了 FIPS 的 OpenSSL 构建的，
这可用于启用符合 FIPS 标准的加密。
.
.It Fl -openssl-legacy-provider
启用 OpenSSL 3.0 旧版提供程序。
更多信息请参见 OSSL_PROVIDER-legacy。

.
.It Fl -openssl-shared-config
启用从 OpenSSL 配置文件中读取 OpenSSL 默认配置部分 \fBopenssl_conf\fR。

默认配置文件名为 \fBopenssl.cnf\fR，但可以使用环境变量 \fBOPENSSL_CONF\fR
或命令行选项 \fB--openssl-config\fR 进行更改。
默认 OpenSSL 配置文件的位置取决于 OpenSSL 如何链接到 Node.js。

共享 OpenSSL 配置可能会产生不良影响，建议使用特定于 Node.js 的配置部分
Bnodejs_conf\fR，
这是未使用此选项时的默认设置。
.
.It Fl -pending-deprecation
发出待处理的弃用警告。
待处理的弃用通常与运行时弃用相同，
但有一个显著区别：它们默认是\fI关闭\fR的，
除非设置了 \fB--pending-deprecation\fR 命令行标志或
\fBNODE_PENDING_DEPRECATION=1\fR 环境变量，否则不会发出。

待处理的弃用用于提供一种选择性的"早期警告"机制，
开发人员可以利用它来检测已弃用的 API 使用。
.
.It Fl -permission
为当前进程启用权限模型。启用后，
以下权限受到限制：
.Bl -bullet
.It
文件系统 - 通过 \fB--allow-fs-read\fR、\fB--allow-fs-write\fR 标志管理
.It
网络 - 通过 \fB--allow-net\fR 标志管理
.It
子进程 - 通过 \fB--allow-child-process\fR 标志管理
.It
工作线程 - 通过 \fB--allow-worker\fR 标志管理
.It
WASI - 通过 \fB--allow-wasi\fR 标志管理
.It
插件 - 通过 \fB--allow-addons\fR 标志管理
.It
FFI - 通过 \fB--allow-ffi\fR 标志管理
.El
.
.It Fl -permission-audit
仅为权限模型启用审计模式。启用后，
权限检查仍会执行，
但不会拒绝访问。
相反，
每条权限违反都会通过诊断通道发出警告。
.
.It Fl -preserve-symlinks
指示模块加载器在解析和缓存模块时保留符号链接。
默认情况下，
当 Node.js 从符号链接到不同磁盘位置的路径加载模块时，
Node.js 将解引用链接，并使用模块的实际磁盘"真实路径"作为标识符和定位其他依赖模块的根路径。

在大多数情况下，此默认行为是可接受的。
但是，
当使用符号链接的对等依赖项时，
如下例所示，
默认行为会导致如果 \fBmoduleA\fR 尝试将 \fBmoduleB\fR 作为对等依赖项加载时抛出异常：
.Bd -literal
{appDir}
 ├── app
 │   ├── index.js
 │   └── node_modules
 │       ├── moduleA -> {appDir}/moduleA
 │       └── moduleB
 │           ├── index.js
 │           └── package.json
 └── moduleA
     ├── index.js
     └── package.json
.Ed
\fB--preserve-symlinks\fR 命令行标志指示 Node.js 使用模块的符号链接路径而非实际路径，
从而允许找到符号链接的对等依赖项。
但请注意，使用 \fB--preserve-symlinks\fR 可能有其他副作用。

具体来说，如果符号链接的\fI原生\fR模块在依赖树中的多个位置被链接，它们可能加载失败
（Node.js 会将这些视为两个不同的模块，并尝试多次加载该模块，从而导致抛出异常）。

B--preserve-symlinks\fR 标志不适用于主模块，
这使得 \fBnode --preserve-symlinks node_module/.bin/<foo>\fR 可以工作。

要对主模块应用相同的行为，还可以使用 \fB--preserve-symlinks-main\fR。

.
.It Fl -preserve-symlinks-main
指示模块加载器在解析和缓存主模块（\fBrequire.main\fR）时保留符号链接。

此标志的存在是为了让主模块可以选择采用 \fB--preserve-symlinks\fR 赋予所有其他导入的相同行为；

但它们是独立的标志，
是为了向后兼容旧版 Node.js。
\fB--preserve-symlinks-main\fR 并不意味着 \fB--preserve-symlinks\fR；

当不希望跟随相对路径解析前的符号链接时，
请将 \fB--preserve-symlinks-main\fR 与 \fB--preserve-symlinks\fR 一起使用。

更多信息请参见 \fB--preserve-symlinks\fR。
.
.It Fl p , Fl -print Ar "script"
与 \fB-e\fR 相同，但打印结果。
.
.It Fl -prof
生成 V8 分析器输出。
.
.It Fl -prof-process
处理使用 V8 选项 \fB--prof\fR 生成的 V8 分析器输出。
.
.It Fl -redirect-warnings Ns = Ns Ar file
将进程警告写入指定文件而不是打印到 stderr。
如果文件不存在则创建，如果存在则追加。
如果在尝试将警告写入文件时发生错误，
警告将改为写入 stderr。
Bfile\fR 名称可以是绝对路径。如果不是，
写入的默认目录由
\fB--diagnostic-dir\fR 命令行选项控制。
.
.It Fl -report-compact
以紧凑格式（单行 JSON）写入报告，
比默认的多行格式更易于日志处理系统消费。
.
.It Fl -report-dir Ns = Ns Ar directory , Fl -report-directory Ns = Ns Ar directory
生成报告的位置。
.
.It Fl -report-exclude-env
传递 \fB--report-exclude-env\fR 时，生成的诊断报告将不包含 \fBenvironmentVariables\fR 数据。

.
.It Fl -report-exclude-network
从诊断报告中排除 \fBheader.networkInterfaces\fR。
默认情况下，此选项未设置，包含网络接口。

.
.It Fl -report-filename Ns = Ns Ar filename
写入报告的文件名。
如果文件名设置为 'stdout' 或 'stderr'，报告将分别写入进程的标准输出或标准错误。

.
.It Fl -report-on-fatalerror
启用在导致应用程序终止的致命错误（Node.js 运行时内部的错误，如内存不足）时触发报告。

可用于检查各种诊断数据元素，如堆、堆栈、事件循环状态、资源消耗等，以分析致命错误。

.
.It Fl -report-on-signal
启用在收到指定（或预定义）信号时生成报告到正在运行的 Node.js 进程。
触发报告的信号通过 \fB--report-signal\fR 指定。
.
.It Fl -report-signal Ns = Ns Ar signal
设置或重置用于生成报告的信号（Windows 上不支持）。
默认信号为 \fBSIGUSR2\fR。

.
.It Fl -report-uncaught-exception
启用在进程因未捕获异常退出时生成报告。
在检查 JavaScript 堆栈以及原生堆栈和其他运行时环境数据时很有用。
.
.It Fl r , Fl -require Ar module
在启动时预加载指定模块。
遵循 \fBrequire()\fR 的模块解析规则。
\fBmodule\fR 可以是文件路径或节点模块名称。

通过 \fB--require\fR 预加载的模块将在通过 \fB--import\fR 预加载的模块之前运行。

模块将在主线程以及任何工作线程、子进程或集群进程中预加载。
.
.It Fl -run
运行 package.json 的 \fB"scripts"\fR 对象中指定的命令。

如果提供了不存在的 \fB"command"\fR，
它将列出可用的脚本。
\fB--run\fR 将向上遍历到根目录并查找 \fBpackage.json\fR 文件来运行命令。

\fB--run\fR 将当前目录每个祖先的 \fB./node_modules/.bin\fR 前置到 \fBPATH\fR 环境变量中，
以便从存在多个 \fBnode_modules\fR 目录的不同文件夹中执行二进制文件，
前提是 \fBancestor-folder/node_modules/.bin\fR 是一个目录。

\fB--run\fR 在包含相关 \fBpackage.json\fR 的目录中执行命令。

例如，以下命令将运行当前文件夹中 \fBpackage.json\fR 的 \fBtest\fR 脚本：
.Bd -literal
$ node --run test
.Ed
您也可以向命令传递参数。\fB--\fR 之后的任何参数都将附加到脚本：
.Bd -literal
$ node --run test -- --verbose
.Ed
.
.It Fl -secure-heap-min Ns = Ns Ar n
使用 \fB--secure-heap\fR 时，\fB--secure-heap-min\fR 标志指定安全堆的最小分配量。

最小值为 \fB2\fR。
最大值为 \fB--secure-heap\fR 和 \fB2147483647\fR 中较小的值。

给定的值必须是 2 的幂。
.
.It Fl -secure-heap Ns = Ns Ar n
初始化一个大小为 \fBn\fR 字节的 OpenSSL 安全堆。初始化后，
安全堆用于 OpenSSL 中在密钥生成和其他操作期间的某些类型分配。
例如，
这可以防止敏感信息因指针溢出或下溢而泄漏。
安全堆是固定大小的，
不能在运行时调整大小，
因此如果使用，
选择足够大的堆以覆盖所有应用程序用途非常重要。
给定的堆大小必须是 2 的幂。任何小于 2 的值将禁用安全堆。
安全堆默认禁用。
安全堆在 Windows 上不可用。
更多详情请参见 \fBCRYPTO_secure_malloc_init\fR。
.
.It Fl -snapshot-blob Ns = Ns Ar path
当与 \fB--build-snapshot\fR 一起使用时，\fB--snapshot-blob\fR 指定生成的快照 blob 写入的路径。

如果未指定，生成的 blob 将写入当前工作目录中的 \fBsnapshot.blob\fR。

当不带 \fB--build-snapshot\fR 使用时，\fB--snapshot-blob\fR 指定用于恢复应用程序状态的 blob 路径。

加载快照时，Node.js 会检查：
.Bl -bullet
.It
运行的 Node.js 二进制文件的版本、架构和平台与生成快照的二进制文件完全相同。

.It
V8 标志和 CPU 特性与生成快照的二进制文件兼容。
.El
如果不匹配，
Node.js 拒绝加载快照并以状态码 1 退出。
.
.It Fl -test
启动 Node.js 命令行测试运行器。
此标志不能与 \fB--watch-path\fR、\fB--check\fR、\fB--eval\fR、
\fB--interactive\fR 或检查器一起使用。
有关更多详细信息，
请参阅从命令行运行测试的文档。
.
.It Fl -test-concurrency
测试运行器 CLI 将同时执行的最大测试文件数。
如果 \fB--test-isolation\fR 设置为 \fB'none'\fR，则忽略此标志，并发数为 1。

否则，并发数默认为 \fBos.availableParallelism() - 1\fR。

.
.It Fl -test-coverage-branches Ns = Ns Ar threshold
要求覆盖的分支达到最小百分比。
如果代码覆盖率未达到指定阈值，进程将以代码 \fB1\fR 退出。

.
.It Fl -test-coverage-exclude
使用 glob 模式从代码覆盖率中排除特定文件，该模式可以匹配绝对和相对文件路径。

可以多次指定此选项以排除多个 glob 模式。
如果同时提供了 \fB--test-coverage-exclude\fR 和 \fB--test-coverage-include\fR，
文件必须满足\fB两个\fR条件才能包含在覆盖率报告中。
默认情况下，
所有匹配的测试文件都从覆盖率报告中排除。
指定此选项将覆盖默认行为。
.
.It Fl -test-coverage-functions Ns = Ns Ar threshold
要求覆盖的函数达到最小百分比。
如果代码覆盖率未达到指定阈值，进程将以代码 \fB1\fR 退出。

.
.It Fl -test-coverage-include
使用 glob 模式在代码覆盖率中包含特定文件，该模式可以匹配绝对和相对文件路径。

可以多次指定此选项以包含多个 glob 模式。
如果同时提供了 \fB--test-coverage-exclude\fR 和 \fB--test-coverage-include\fR，
文件必须满足\fB两个\fR条件才能包含在覆盖率报告中。
.
.It Fl -test-coverage-lines Ns = Ns Ar threshold
要求覆盖的行达到最小百分比。
如果代码覆盖率未达到指定阈值，进程将以代码 \fB1\fR 退出。

.
.It Fl -test-force-exit
配置测试运行器在所有已知测试执行完毕后退出进程，
即使事件循环本应保持活动状态。
.
.It Fl -test-global-setup Ns = Ns Ar module
指定在所有测试执行前求值的模块，
可用于设置全局状态或测试装置。
有关更多详细信息，
请参阅全局设置和拆卸的文档。
.
.It Fl -test-isolation Ns = Ns Ar mode
配置测试运行器中使用的测试隔离类型。
当 \fBmode\fR 为 \fB'process'\fR 时，
每个测试文件在单独的子进程中运行。
当 \fBmode\fR 为 \fB'none'\fR 时，
所有测试文件在与测试运行器相同的进程中运行。
默认隔离模式为 \fB'process'\fR。

如果不存在 \fB--test\fR 标志，则忽略此标志。
有关更多信息，请参阅测试运行器执行模型部分。

.
.It Fl -test-name-pattern
一个正则表达式，
配置测试运行器仅执行名称与提供的模式匹配的测试。
有关更多详细信息，
请参阅按名称过滤测试的文档。
如果同时提供了 \fB--test-name-pattern\fR 和 \fB--test-skip-pattern\fR，
测试必须满足\fB两个\fB条件才能执行。
.
.It Fl -test-only
配置测试运行器仅执行设置了 \fBonly\fR 选项的顶层测试。
当测试隔离禁用时，不需要此标志。
.
.It Fl -test-random-seed
设置用于随机化测试执行顺序的种子。
这适用于测试文件执行顺序和每个文件中的队列测试。

提供此标志会隐式启用随机化，即使没有 \fB--test-randomize\fR 也是如此。

该值必须是一个介于 \fB0\fR 和 \fB4294967295\fR 之间的整数。
此标志不能与 \fB--watch\fR 或 \fB--test-rerun-failures\fR 一起使用。

.
.It Fl -test-randomize
随机化测试执行顺序。这适用于测试文件执行顺序和每个文件中的队列测试。
这有助于检测依赖于共享状态或执行顺序的测试。
用于随机化的种子会打印在测试摘要中，
并可以
通过 \fB--test-random-seed\fR 重复使用。
有关详细行为示例，
请参阅随机化测试执行顺序。
此标志不能与 \fB--watch\fR 或 \fB--test-rerun-failures\fR 一起使用。

.
.It Fl -test-reporter
运行测试时使用的测试报告器。有关更多详细信息，
请参阅测试报告器的文档。
.
.It Fl -test-reporter-destination
相应测试报告器的目标。有关更多详细信息，
请参阅测试报告器的文档。
.
.It Fl -test-rerun-failures
允许测试运行器在运行之间持久化测试套件状态的文件路径。
测试运行器将使用此文件确定哪些测试已成功或失败，
从而允许重新运行失败的测试，
而无需重新运行整个测试套件。如果文件不存在，
测试运行器将创建它。
有关更多详细信息，
请参阅测试重新运行的文档。
.
.It Fl -test-shard
以 \fB<index>/<total>\fR 格式执行的测试套件分片，
其中
.Bl -bullet
.It
Bindex\fR 是正整数，
表示分片部分的索引。
.It
Btotal\fR 是正整数，
表示分片部分的总数。
.El
此命令将把所有测试文件分成 \fBtotal\fR 个相等的部分，
并且仅运行恰好属于 \fBindex\fR 部分的那些文件。
例如，要将测试套件分成三部分，请使用：
.Bd -literal
node --test --test-shard=1/3
node --test --test-shard=2/3
node --test --test-shard=3/3
.Ed
.
.It Fl -test-skip-pattern
一个正则表达式，
配置测试运行器跳过名称与提供的模式匹配的测试。
有关更多详细信息，
请参阅按名称过滤测试的文档。
如果同时提供了 \fB--test-name-pattern\fR 和 \fB--test-skip-pattern\fR，
测试必须满足\fB两个\fB条件才能执行。
.
.It Fl -test-timeout
测试执行将在多少毫秒后失败。
如果未指定，子测试从父测试继承此值。
默认值为 \fBInfinity\fR。

.
.It Fl -test-update-snapshots
重新生成测试运行器用于快照测试的快照文件。
.
.It Fl -throw-deprecation
对弃用抛出错误。
.
.It Fl -title Ns = Ns Ar title
在启动时设置 \fBprocess.title\fR。
.
.It Fl -tls-cipher-list Ns = Ns Ar list
指定替代的默认 TLS 密码列表。
需要 Node.js 使用加密支持构建（默认）。

.
.It Fl -tls-keylog Ns = Ns Ar file
将 TLS 密钥材料记录到文件。
密钥材料采用 NSS \fBSSLKEYLOGFILE\fR 格式，
可以被 Wireshark 等软件用于解密 TLS 流量。
.
.It Fl -tls-max-v1.2
将 \fBtls.DEFAULT_MAX_VERSION\fR 设置为 'TLSv1.2'。
用于禁用 TLSv1.3 支持。

.
.It Fl -tls-max-v1.3
将默认 \fBtls.DEFAULT_MAX_VERSION\fR 设置为 'TLSv1.3'。
用于启用 TLSv1.3 支持。

.
.It Fl -tls-min-v1.0
将默认 \fBtls.DEFAULT_MIN_VERSION\fR 设置为 'TLSv1'。
用于与旧的 TLS 客户端或服务器兼容。

.
.It Fl -tls-min-v1.1
将默认 \fBtls.DEFAULT_MIN_VERSION\fR 设置为 'TLSv1.1'。
用于与旧的 TLS 客户端或服务器兼容。

.
.It Fl -tls-min-v1.2
将默认 \fBtls.DEFAULT_MIN_VERSION\fR 设置为 'TLSv1.2'。
这是 12.x 及更高版本的默认值，
但支持此选项是为了与旧版 Node.js 兼容。
.
.It Fl -tls-min-v1.3
将默认 \fBtls.DEFAULT_MIN_VERSION\fR 设置为 'TLSv1.3'。
用于禁用不如 TLSv1.3 安全的 TLSv1.2 支持。

.
.It Fl -trace-deprecation
打印弃用的堆栈跟踪。
.
.It Fl -trace-env
打印关于当前 Node.js 实例中对环境变量所做的任何访问的信息到 stderr，包括：
.Bl -bullet
.It
Node.js 内部进行的环境变量读取。
.It
以 \fBprocess.env.KEY = "SOME VALUE"\fR 形式的写入。

.It
以 \fBprocess.env.KEY\fR 形式的读取。
.It
以 \fBObject.defineProperty(process.env, 'KEY', {...})\fR 形式的定义。

.It
以 \fBObject.hasOwn(process.env, 'KEY')\fR、
\fBprocess.env.hasOwnProperty('KEY')\fR 或 \fB'KEY' in process.env\fR 形式的查询。

.It
以 \fBdelete process.env.KEY\fR 形式的删除。
.It
以 \fB...process.env\fR 或 \fBObject.keys(process.env)\fR 形式的枚举。

.El
仅打印正在访问的环境变量的名称。不打印值。
要打印访问的堆栈跟踪，请使用 \fB--trace-env-js-stack\fR 和/或 \fB--trace-env-native-stack\fR。

.
.It Fl -trace-env-js-stack
除了 \fB--trace-env\fR 的功能外，还打印访问的 JavaScript 堆栈跟踪。

.
.It Fl -trace-env-native-stack
除了 \fB--trace-env\fR 的功能外，
还打印访问的原生堆栈跟踪。
.
.It Fl -trace-event-categories
使用 \fB--trace-events-enabled\fR 启用跟踪事件时，应跟踪的以逗号分隔的类别列表。

.
.It Fl -trace-event-file-pattern
指定跟踪事件数据文件路径的模板字符串，
支持 \fB${rotation}\fR 和 \fB${pid}\fR。
.
.It Fl -trace-events-enabled
启用收集跟踪事件信息。
.
.It Fl -trace-exit
每当环境被主动退出时打印堆栈跟踪，
即调用 \fBprocess.exit()\fR。
.
.It Fl -trace-require-module Ns = Ns Ar mode
打印关于使用 \fBrequire()\fR 加载 ECMAScript 模块的信息。

当 \fBmode\fR 为 \fBall\fR 时，打印所有使用情况。
当 \fBmode\fR 为 \fBno-node-modules\fR 时，
排除来自 \fBnode_modules\fR 文件夹的使用。
.
.It Fl -trace-sigint
在 SIGINT 时打印堆栈跟踪。
.
.It Fl -trace-sync-io
在事件循环的第一个轮次之后检测到同步 I/O 时打印堆栈跟踪。
.
.It Fl -trace-tls
将 TLS 数据包跟踪信息打印到 \fBstderr\fR。
可用于调试 TLS 连接问题。

.
.It Fl -trace-uncaught
打印未捕获异常的堆栈跟踪；
通常，与创建 \fBError\fR 相关的堆栈跟踪会被打印，
而此选项使 Node.js 也打印与抛出值（不需要是 \fBError\fR 实例）相关的堆栈跟踪。

启用此选项可能会对垃圾回收行为产生负面影响。
.
.It Fl -trace-warnings
打印进程警告的堆栈跟踪（包括弃用）。
.
.It Fl -track-heap-objects
跟踪堆对象分配以生成堆快照。
.
.It Fl -unhandled-rejections Ns = Ns Ar mode
使用此标志可以更改发生未处理拒绝时的行为。可以选择以下模式之一：
.Bl -bullet
.It
\fBthrow\fR：发出 \fBunhandledRejection\fR。
如果未设置此钩子，将未处理拒绝作为未捕获异常抛出。
这是默认值。

.It
\fBstrict\fR：将未处理拒绝作为未捕获异常抛出。
如果异常被处理，则发出 \fBunhandledRejection\fR。

.It
\fBwarn\fR：始终触发警告，无论是否设置了 \fBunhandledRejection\fR 钩子，但不打印弃用警告。

.It
\fBwarn-with-error-code\fR：发出 \fBunhandledRejection\fR。
如果未设置此钩子，触发警告并将进程退出代码设置为 1。

.It
\fBnone\fR：静默所有警告。
.El
如果在命令行入口点的 ES 模块静态加载阶段发生拒绝，它将始终作为未捕获异常抛出。

.
.It Fl -use-bundled-ca , Fl -use-openssl-ca
使用当前 Node.js 版本提供的捆绑 Mozilla CA 存储，
或使用 OpenSSL 的默认 CA 存储。默认存储可以在构建时选择。
Node.js 提供的捆绑 CA 存储在发布时是 Mozilla CA 存储的快照。

它在所有受支持的平台上都是相同的。
使用 OpenSSL 存储允许对存储进行外部修改。
对于大多数 Linux 和 BSD 发行版，
此存储由发行版维护者和系统管理员维护。
OpenSSL CA 存储位置取决于 OpenSSL 库的配置，但可以在运行时使用环境变量更改。

请参见 \fBSSL_CERT_DIR\fR 和 \fBSSL_CERT_FILE\fR。

.
.It Fl -use-env-proxy
启用后，Node.js 在启动时解析 \fBHTTP_PROXY\fR、\fBHTTPS_PROXY\fR 和 \fBNO_PROXY\fR
环境变量，并通过指定的代理隧道请求。
这相当于设置 \fBNODE_USE_ENV_PROXY=1\fR 环境变量。
当两者都设置时，
B--use-env-proxy\fR 优先。，
.
.It Fl -use-largepages Ns = Ns Ar mode
在启动时将 Node.js 静态代码重新映射到大内存页面。
如果在目标系统上受支持，
这将使 Node.js 静态代码移至 2 MiB 页面而不是 4 KiB 页面。
以下 \fBmode\fR 值有效：
.Bl -bullet
.It
\fBoff\fR：不尝试映射。这是默认值。
.It
\fBon\fR：如果操作系统支持，将尝试映射。
映射失败将被忽略，并向标准错误打印一条消息。

.It
Bsilent\fR：如果操作系统支持，
将尝试映射。映射失败将被忽略且不会报告。
.El
.
.It Fl -use-system-ca
Node.js 使用系统存储中存在的受信任 CA 证书，
以及
\fB--use-bundled-ca\fR 选项和 \fBNODE_EXTRA_CA_CERTS\fR 环境变量。

在 Windows 和 macOS 以外的平台上，这会从 OpenSSL 信任的目录和文件加载证书，
类似于 \fB--use-openssl-ca\fR，区别在于它在首次加载后缓存证书。

在 Windows 和 macOS 上，证书信任策略类似于 Chromium 的本地受信任证书策略，
但有一些差异：
在 macOS 上，遵循以下设置：
.Bl -bullet
.It
默认和系统钥匙串。.Bl -bullet
.It
信任：.Bl -bullet
.It
任何将 "When using this certificate" 标志设置为 "Always Trust" 的证书，或
.It
任何将 "Secure Sockets Layer (SSL)" 标志设置为 "Always Trust" 的证书。

.El
.It
证书还必须有效，且 "X.509 Basic Policy" 设置为 "Always Trust"。

.El
.El
在 Windows 上，遵循以下设置：
.Bl -bullet
.It
本地计算机（通过 \fBcertlm.msc\fR 访问）。
.Bl -bullet
.It
信任：
.Bl -bullet
.It
受信任的根证书颁发机构
.It
受信任的人员
.It
企业信任 -> 企业 -> 受信任的根证书颁发机构
.It
企业信任 -> 企业 -> 受信任的人员
.It
企业信任 -> 组策略 -> 受信任的根证书颁发机构
.It
企业信任 -> 组策略 -> 受信任的人员
.El
.El
.It
当前用户（通过 \fBcertmgr.msc\fR 访问）。
.Bl -bullet
.It
信任：
.Bl -bullet
.It
受信任的根证书颁发机构
.It
企业信任 -> 组策略 -> 受信任的根证书颁发机构
.El
.El
.El
在 Windows 和 macOS 上，Node.js 将检查用户对受信任证书的设置，在使用它们进行 TLS 服务器身份验证之前不会阻止它们。

Node.js 目前不支持基于系统设置从其他来源取消信任/撤销证书。
在其他系统上，Node.js 从 Node.js 链接的 OpenSSL 版本所认可的默认证书文件
（通常为 \fB/etc/ssl/cert.pem\fR）和默认证书目录（通常为 \fB/etc/ssl/certs\fR）加载证书。

这通常适用于主要 Linux 发行版和其他类 Unix 系统的约定。
如果设置了覆盖的 OpenSSL 环境变量（通常为 \fBSSL_CERT_FILE\fR 和 \fBSSL_CERT_DIR\fR，
取决于 Node.js 链接的 OpenSSL 配置），将使用指定的路径加载证书。

如果 Node.js 链接的 OpenSSL 版本使用的传统路径因某种原因与用户的系统配置不一致，
这些环境变量可以用作解决方法。
.
.It Fl -v8-options
打印 V8 命令行选项。
.
.It Fl -v8-pool-size Ns = Ns Ar num
设置 V8 的线程池大小，
用于分配后台作业。
如果设置为 \fB0\fR，Node.js 将根据对并行度的估计选择合适的线程池大小。

并行度指在给定机器上可以同时执行的计算数量。通常它与 CPU 数量相同，
但在 VM 或容器等环境中可能有所不同。
.
.It Fl v , Fl -version
打印 node 的版本。
.
.It Fl -watch
以监视模式启动 Node.js。
在监视模式下，
被监视文件的变化会导致 Node.js 进程重启。
默认情况下，监视模式将监视入口点以及任何被 require 或 import 的模块。

使用 \fB--watch-path\fR 指定要监视的路径。
此标志不能与 \fB--check\fR、\fB--eval\fR、\fB--interactive\fR 或 REPL 结合使用。

注意：\fB--watch\fR 标志需要文件路径作为参数，并且与 \fB--run\fR 或内联脚本输入不兼容，
因为 \fB--run\fR 优先并忽略监视模式。
如果未提供文件，Node.js 将以状态码 \fB9\fR 退出。

.Bd -literal
node --watch index.js
.Ed
.
.It Fl -watch-kill-signal
自定义在监视模式重启时发送给进程的信号。
.Bd -literal
node --watch --watch-kill-signal SIGINT test.js
.Ed
.
.It Fl -watch-path
以监视模式启动 Node.js 并指定要监视的路径。
在监视模式下，
被监视路径的变化会导致 Node.js 进程重启。
即使与 \fB--watch\fR 结合使用，这也会关闭对 require 或 import 模块的监视。

此标志不能与 \fB--check\fR、\fB--eval\fR、\fB--interactive\fR、\fB--test\fR 或 REPL 结合使用。

注意：使用 \fB--watch-path\fR 会隐式启用 \fB--watch\fR，这需要文件路径，
并且与 \fB--run\fR 不兼容，因为 \fB--run\fR 优先并忽略监视模式。

.Bd -literal
node --watch-path=./src --watch-path=./tests index.js
.Ed
此选项仅在 macOS 和 Windows 上受支持。
当在不支持此选项的平台上使用时，将抛出 \fBERR_FEATURE_UNAVAILABLE_ON_PLATFORM\fR 异常。

.
.It Fl -watch-preserve-output
禁止在监视模式重启进程时清除控制台。
.Bd -literal
node --watch --watch-preserve-output test.js
.Ed
.
.It Fl -zero-fill-buffers
自动对所有新分配的 \fBBuffer\fR 实例进行零填充。
.

.El
.
.Sh 环境变量
.Bl -tag -width 6n
.It Ev FORCE_COLOR Ar [1, 2, 3]
\fBFORCE_COLOR\fR 环境变量用于启用 ANSI 彩色输出。值可以是：
.Bl -bullet
.It
B1\fR、\fBtrue\fR 或空字符串 \fB''\fR 表示 16 色支持，
.It
\fB2\fR 表示 256 色支持，或
.It
\fB3\fR 表示 1600 万色支持。
.El
当使用 \fBFORCE_COLOR\fR 并设置为支持的值时，\fBNO_COLOR\fR
和 \fBNODE_DISABLE_COLORS\fR 环境变量都将被忽略。
任何其他值都会导致彩色输出被禁用。
.
.It Ev NODE_COMPILE_CACHE Ar dir
为 Node.js 实例启用模块编译缓存。
有关详细信息，请参阅模块编译缓存的文档。

.
.It Ev NODE_COMPILE_CACHE_PORTABLE Ar 1
当设置为 1 时，
只要模块相对于缓存目录的布局保持不变，
模块编译缓存可以在不同目录位置之间重复使用。
.
.It Ev NODE_DEBUG Ar module[,…]
以 \fB','\fR 分隔的核心模块列表，
这些模块应打印调试信息。
.
.It Ev NODE_DEBUG_NATIVE Ar module[,…]
以 \fB','\fR 分隔的核心 C++ 模块列表，
这些模块应打印调试信息。
.
.It Ev NODE_DISABLE_COLORS Ar 1
设置后，REPL 中将不使用颜色。
.
.It Ev NODE_DISABLE_COMPILE_CACHE Ar 1
禁用 Node.js 实例的模块编译缓存。
有关详细信息，请参阅模块编译缓存的文档。

.
.It Ev NODE_EXTRA_CA_CERTS Ar file
设置后，众所周知的 "root" CA（如 VeriSign）将扩展为包含 \fBfile\fR 中的额外证书。

文件应包含一个或多个 PEM 格式的受信任证书。
如果文件缺失或格式错误，将通过 \fBprocess.emitWarning()\fR 发出消息（一次），
但其他错误将被忽略。
当为 TLS 或 HTTPS 客户端或服务器显式指定 \fBca\fR 选项属性时，
既不会使用众所周知的证书也不会使用额外证书。
当 \fBnode\fR 以 setuid root 运行或设置了 Linux 文件能力时，此环境变量被忽略。

\fBNODE_EXTRA_CA_CERTS\fR 环境变量仅在首次启动 Node.js 进程时读取。

在运行时使用 \fBprocess.env.NODE_EXTRA_CA_CERTS\fR 更改值对当前进程没有影响。

.
.It Ev NODE_ICU_DATA Ar file
ICU（\fBIntl\fR 对象）数据的路径。
在使用 small-icu 支持编译时会扩展链接的数据。

.
.It Ev NODE_NO_WARNINGS Ar 1
当设置为 \fB1\fR 时，
进程警告被静默。
.
.It Ev NODE_OPTIONS Ar options...
以空格分隔的命令行选项列表。
\fBoptions...\fR 在命令行选项之前被解释，
因此命令行选项将覆盖或复合 \fBoptions...\fR 之后的任何内容。
如果使用了环境中不允许的选项（如 \fB-p\fR 或脚本文件），Node.js 将以错误退出。

如果选项值包含空格，
可以使用双引号进行转义：
.Bd -literal
NODE_OPTIONS='--require "./my path/file.js"'
.Ed
作为命令行选项传入的单例标志将覆盖传入 \fBNODE_OPTIONS\fR 的相同标志：
.Bd -literal
# 检查器将在端口 5555 上可用
NODE_OPTIONS='--inspect=localhost:4444' node --inspect=localhost:5555
.Ed
可以多次传递的标志将被视为先传递 \fBNODE_OPTIONS\fR 实例，
然后再传递命令行实例：
.Bd -literal
NODE_OPTIONS='--require "./a.js"' node --require "./b.js"
# 等效于：
node --require "./a.js" --require "./b.js"
.Ed
允许的 Node.js 选项如下列表所示。
如果一个选项同时支持 --XX 和 --no-XX 变体，
则两者都受支持，但下面列表中只包含一个。
.Bl -bullet
.It
\fB--allow-addons\fR
.It
\fB--allow-child-process\fR
.It
\fB--allow-ffi\fR
.It
\fB--allow-fs-read\fR
.It
\fB--allow-fs-write\fR
.It
\fB--allow-inspector\fR
.It
\fB--allow-net\fR
.It
\fB--allow-wasi\fR
.It
\fB--allow-worker\fR
.It
\fB--conditions\fR, \fB-C\fR
.It
\fB--cpu-prof-dir\fR
.It
\fB--cpu-prof-interval\fR
.It
\fB--cpu-prof-name\fR
.It
\fB--cpu-prof\fR
.It
\fB--diagnostic-dir\fR
.It
\fB--disable-proto\fR
.It
\fB--disable-sigusr1\fR
.It
\fB--disable-warning\fR
.It
\fB--disable-wasm-trap-handler\fR
.It
\fB--dns-result-order\fR
.It
\fB--enable-fips\fR
.It
\fB--enable-network-family-autoselection\fR
.It
\fB--enable-source-maps\fR
.It
\fB--entry-url\fR
.It
\fB--experimental-abortcontroller\fR
.It
\fB--experimental-addon-modules\fR
.It
\fB--experimental-detect-module\fR
.It
\fB--experimental-eventsource\fR
.It
\fB--experimental-ffi\fR
.It
\fB--experimental-import-meta-resolve\fR
.It
\fB--experimental-json-modules\fR
.It
\fB--experimental-loader\fR
.It
\fB--experimental-modules\fR
.It
\fB--experimental-package-map\fR
.It
\fB--experimental-print-required-tla\fR
.It
\fB--experimental-quic\fR
.It
\fB--experimental-require-module\fR
.It
\fB--experimental-shadow-realm\fR
.It
\fB--experimental-specifier-resolution\fR
.It
\fB--experimental-stream-iter\fR
.It
\fB--experimental-test-isolation\fR
.It
\fB--experimental-top-level-await\fR
.It
\fB--experimental-vfs\fR
.It
\fB--experimental-vm-modules\fR
.It
\fB--experimental-wasi-unstable-preview1\fR
.It
\fB--force-context-aware\fR
.It
\fB--force-fips\fR
.It
\fB--force-node-api-uncaught-exceptions-policy\fR
.It
\fB--frozen-intrinsics\fR
.It
\fB--heap-prof-dir\fR
.It
\fB--heap-prof-interval\fR
.It
\fB--heap-prof-name\fR
.It
\fB--heap-prof\fR
.It
\fB--heapsnapshot-near-heap-limit\fR
.It
\fB--heapsnapshot-signal\fR
.It
\fB--http-parser\fR
.It
\fB--icu-data-dir\fR
.It
\fB--import\fR
.It
\fB--input-type\fR
.It
\fB--insecure-http-parser\fR
.It
\fB--inspect-brk\fR
.It
\fB--inspect-port\fR, \fB--debug-port\fR
.It
\fB--inspect-publish-uid\fR
.It
\fB--inspect-wait\fR
.It
\fB--inspect\fR
.It
\fB--localstorage-file\fR
.It
\fB--max-http-header-size\fR
.It
\fB--max-old-space-size-percentage\fR
.It
\fB--napi-modules\fR
.It
\fB--network-family-autoselection-attempt-timeout\fR
.It
\fB--no-addons\fR
.It
\fB--no-async-context-frame\fR
.It
\fB--no-deprecation\fR
.It
\fB--no-experimental-global-navigator\fR
.It
\fB--no-experimental-repl-await\fR
.It
\fB--no-experimental-sqlite\fR
.It
\fB--no-experimental-strip-types\fR
.It
\fB--no-experimental-websocket\fR
.It
\fB--no-experimental-webstorage\fR
.It
\fB--no-extra-info-on-fatal-exception\fR
.It
\fB--no-force-async-hooks-checks\fR
.It
\fB--no-global-search-paths\fR
.It
\fB--no-network-family-autoselection\fR
.It
\fB--no-strip-types\fR
.It
\fB--no-warnings\fR
.It
\fB--no-webstorage\fR
.It
\fB--node-memory-debug\fR
.It
\fB--openssl-config\fR
.It
\fB--openssl-legacy-provider\fR
.It
\fB--openssl-shared-config\fR
.It
\fB--pending-deprecation\fR
.It
\fB--permission-audit\fR
.It
\fB--permission\fR
.It
\fB--preserve-symlinks-main\fR
.It
\fB--preserve-symlinks\fR
.It
\fB--prof-process\fR
.It
\fB--redirect-warnings\fR
.It
\fB--report-compact\fR
.It
\fB--report-dir\fR, \fB--report-directory\fR
.It
\fB--report-exclude-env\fR
.It
\fB--report-exclude-network\fR
.It
\fB--report-filename\fR
.It
\fB--report-on-fatalerror\fR
.It
\fB--report-on-signal\fR
.It
\fB--report-signal\fR
.It
\fB--report-uncaught-exception\fR
.It
\fB--require-module\fR
.It
\fB--require\fR, \fB-r\fR
.It
\fB--secure-heap-min\fR
.It
\fB--secure-heap\fR
.It
\fB--snapshot-blob\fR
.It
\fB--test-coverage-branches\fR
.It
\fB--test-coverage-exclude\fR
.It
\fB--test-coverage-functions\fR
.It
\fB--test-coverage-include\fR
.It
\fB--test-coverage-lines\fR
.It
\fB--test-global-setup\fR
.It
\fB--test-isolation\fR
.It
\fB--test-name-pattern\fR
.It
\fB--test-only\fR
.It
\fB--test-random-seed\fR
.It
\fB--test-randomize\fR
.It
\fB--test-reporter-destination\fR
.It
\fB--test-reporter\fR
.It
\fB--test-rerun-failures\fR
.It
\fB--test-shard\fR
.It
\fB--test-skip-pattern\fR
.It
\fB--throw-deprecation\fR
.It
\fB--title\fR
.It
\fB--tls-cipher-list\fR
.It
\fB--tls-keylog\fR
.It
\fB--tls-max-v1.2\fR
.It
\fB--tls-max-v1.3\fR
.It
\fB--tls-min-v1.0\fR
.It
\fB--tls-min-v1.1\fR
.It
\fB--tls-min-v1.2\fR
.It
\fB--tls-min-v1.3\fR
.It
\fB--trace-deprecation\fR
.It
\fB--trace-env-js-stack\fR
.It
\fB--trace-env-native-stack\fR
.It
\fB--trace-env\fR
.It
\fB--trace-event-categories\fR
.It
\fB--trace-event-file-pattern\fR
.It
\fB--trace-events-enabled\fR
.It
\fB--trace-exit\fR
.It
\fB--trace-require-module\fR
.It
\fB--trace-sigint\fR
.It
\fB--trace-sync-io\fR
.It
\fB--trace-tls\fR
.It
\fB--trace-uncaught\fR
.It
\fB--trace-warnings\fR
.It
\fB--track-heap-objects\fR
.It
\fB--unhandled-rejections\fR
.It
\fB--use-bundled-ca\fR
.It
\fB--use-env-proxy\fR
.It
\fB--use-largepages\fR
.It
\fB--use-openssl-ca\fR
.It
\fB--use-system-ca\fR
.It
\fB--v8-pool-size\fR
.It
\fB--watch-kill-signal\fR
.It
\fB--watch-path\fR
.It
\fB--watch-preserve-output\fR
.It
\fB--watch\fR
.It
\fB--zero-fill-buffers\fR
.El
允许的 V8 选项有：
.Bl -bullet
.It
\fB--abort-on-uncaught-exception\fR
.It
\fB--disallow-code-generation-from-strings\fR
.It
\fB--enable-etw-stack-walking\fR
.It
\fB--expose-gc\fR
.It
\fB--interpreted-frames-native-stack\fR
.It
\fB--jitless\fR
.It
\fB--max-heap-size\fR
.It
\fB--max-old-space-size\fR
.It
\fB--max-semi-space-size\fR
.It
\fB--perf-basic-prof-only-functions\fR
.It
\fB--perf-basic-prof\fR
.It
\fB--perf-prof-unwinding-info\fR
.It
\fB--perf-prof\fR
.It
\fB--stack-trace-limit\fR
.El
\fB--perf-basic-prof-only-functions\fR、\fB--perf-basic-prof\fR、
\fB--perf-prof-unwinding-info\fR 和 \fB--perf-prof\fR 仅在 Linux 上可用。

\fB--enable-etw-stack-walking\fR 仅在 Windows 上可用。

.
.It Ev NODE_PATH Ar path[:...]
以 \fB':'\fR 分隔的目录列表，
前置到模块搜索路径。
在 Windows 上，
这是以 \fB';'\fR 分隔的列表。
.
.It Ev NODE_PENDING_DEPRECATION Ar 1
当设置为 \fB1\fR 时，
发出待处理的弃用警告。
待处理的弃用通常与运行时弃用相同，
但有一个显著区别：它们默认是\fI关闭\fR的，
除非设置了 \fB--pending-deprecation\fR 命令行标志或
\fBNODE_PENDING_DEPRECATION=1\fR 环境变量，否则不会发出。

待处理的弃用用于提供一种选择性的"早期警告"机制，
开发人员可以利用它来检测已弃用的 API 使用。
.
.It Ev NODE_PENDING_PIPE_INSTANCES Ar instances
设置管道服务器等待连接时的待处理管道实例句柄数。此设置仅适用于 Windows。
.
.It Ev NODE_PRESERVE_SYMLINKS Ar 1
当设置为 \fB1\fR 时，
指示模块加载器在解析和缓存模块时保留符号链接。
.
.It Ev NODE_REDIRECT_WARNINGS Ar file
设置后，
进程警告将被发送到给定文件而不是打印到 stderr。
如果文件不存在则创建，如果存在则追加。
如果在尝试将警告写入文件时发生错误，
警告将改为写入 stderr。
这相当于使用 \fB--redirect-warnings=file\fR 命令行标志。

.
.It Ev NODE_REPL_EXTERNAL_MODULE Ar file
将替换内置 REPL 加载的 Node.js 模块的路径。
将此值覆盖为空字符串（''）将使用内置 REPL。
.
.It Ev NODE_REPL_HISTORY Ar file
用于存储持久化 REPL 历史记录的文件的路径。默认路径为
B~/.node_repl_history\fR，
此变量会覆盖它。
将值设置为空字符串（'' 或 ' '）会禁用持久化 REPL 历史记录。
.
.It Ev NODE_SKIP_PLATFORM_CHECK Ar value
如果 \fBvalue\fR 等于 \fB'1'\fR，则在 Node.js 启动时跳过受支持平台的检查。

Node.js 可能无法正确执行。在不支持的平台上遇到的任何问题都不会被修复。
.
.It Ev NODE_TEST_CONTEXT Ar value
如果 \fBvalue\fR 等于 \fB'child'\fR，将覆盖测试报告器选项，测试输出将以 TAP 格式发送到 stdout。

如果提供了任何其他值，
Node.js 不保证报告器格式或稳定性。
.
.It Ev NODE_TLS_REJECT_UNAUTHORIZED Ar value
如果 \fBvalue\fR 等于 \fB'0'\fR，
TLS 连接的证书验证被禁用。
这会使 TLS 以及 HTTPS 变得不安全。强烈不鼓励使用此环境变量。
.
.It Ev NODE_USE_ENV_PROXY Ar 1
启用后，Node.js 在启动时解析 \fBHTTP_PROXY\fR、\fBHTTPS_PROXY\fR 和 \fBNO_PROXY\fR
环境变量，并通过指定的代理隧道请求。
这也可以通过 \fB--use-env-proxy\fR 命令行标志启用。
当两者都设置时，
B--use-env-proxy\fR 优先。，
.
.It Ev NODE_USE_SYSTEM_CA Ar 1
Node.js 使用系统存储中存在的受信任 CA 证书，
以及
\fB--use-bundled-ca\fR 选项和 \fBNODE_EXTRA_CA_CERTS\fR 环境变量。

这也可以通过 \fB--use-system-ca\fR 命令行标志启用。
当两者都设置时，
B--use-system-ca\fR 优先。，
.
.It Ev NODE_V8_COVERAGE Ar dir
设置后，
Node.js 将开始输出 V8 JavaScript 代码覆盖率和
Source Map 数据到作为参数提供的目录（覆盖率信息作为 JSON 写入以 \fBcoverage\fR 为前缀的文件）。

BNODE_V8_COVERAGE\fR 将自动传播到子进程，
使检测调用
\fBchild_process.spawn()\fR 系列函数的应用程序更加容易。
BNODE_V8_COVERAGE\fR 可以设置为空字符串，
以防止传播。
.
.It Ev NO_COLOR Ar <any>
\fBNO_COLOR\fR 是 \fBNODE_DISABLE_COLORS\fR 的别名。
环境变量的值可以是任意的。

.
.It Ev OPENSSL_CONF Ar file
在启动时加载 OpenSSL 配置文件。除其他用途外，
如果 Node.js 使用
\fB./configure --openssl-fips\fR 构建，这可用于启用符合 FIPS 标准的加密。

如果使用了 \fB--openssl-config\fR 命令行选项，则忽略环境变量。

.
.It Ev SSL_CERT_DIR Ar dir
如果启用了 \fB--use-openssl-ca\fR，或者如果启用了 \fB--use-system-ca\fR
在 macOS 和 Windows 以外的平台上，这会覆盖并设置包含受信任证书的 OpenSSL 目录。

请注意，
除非显式设置了子环境，
否则此环境变量将被所有子进程继承，
如果它们使用 OpenSSL，
可能会导致它们信任与 node 相同的 CA。
.
.It Ev SSL_CERT_FILE Ar file
如果启用了 \fB--use-openssl-ca\fR，或者如果启用了 \fB--use-system-ca\fR
在 macOS 和 Windows 以外的平台上，这会覆盖并设置包含受信任证书的 OpenSSL 文件。

请注意，
除非显式设置了子环境，
否则此环境变量将被所有子进程继承，
如果它们使用 OpenSSL，
可能会导致它们信任与 node 相同的 CA。
.
.It Ev TZ
\fBTZ\fR 环境变量用于指定时区配置。
虽然 Node.js 不支持其他环境中处理 \fBTZ\fR 的所有各种方式，
但它支持基本的时区 ID（如 \fB'Etc/UTC'\fR、\fB'Europe/Paris'\fR 或 \fB'America/New_York'\fR）。

它可能支持一些其他缩写或别名，
但这些强烈不鼓励且不保证。
.Bd -literal
$ TZ=Europe/Dublin node -pe "new Date().toString()"
Wed May 12 2021 20:30:48 GMT+0100 (Irish Standard Time)
.Ed
.
.It Ev UV_THREADPOOL_SIZE Ar size
将 libuv 线程池中使用的线程数设置为 \fBsize\fR 个线程。
Node.js 尽可能使用异步系统 API，
但在不存在这些 API 的地方，
libuv 的线程池用于基于同步系统 API 创建异步 node API。
使用线程池的 Node.js API 有：
.Bl -bullet
.It
所有 \fBfs\fR API，
文件监视器 API 和显式同步的 API 除外
.It
异步加密 API，如 \fBcrypto.pbkdf2()\fR、\fBcrypto.scrypt()\fR、
\fBcrypto.randomBytes()\fR、\fBcrypto.randomFill()\fR、\fBcrypto.generateKeyPair()\fR
.It
\fBdns.lookup()\fR
.It
所有 \fBzlib\fR API，
显式同步的 API 除外
.El
由于 libuv 的线程池具有固定大小，这意味着如果由于某种原因这些 API 中的任何一个需要很长时间，
在线程池中运行的其他（看似无关的）API 将经历性能下降。
为了缓解此问题，一个潜在的解决方案是通过将 'UV_THREADPOOL_SIZE' 环境变量设置为大于 \fB4\fR（其当前默认值）的值
来增加 libuv 线程池的大小。
但是，从进程内部使用 \fBprocess.env.UV_THREADPOOL_SIZE=size\fR 进行设置不能保证生效，
因为线程池将在用户代码运行之前很久就作为运行时初始化的一部分创建。
有关更多信息，
请参阅 libuv 线程池文档。
.

.El
.
.Sh 错误
Bug 在 GitHub Issues 中跟踪：
.Sy https://github.com/nodejs/node/issues
.
.Sh 版权
版权归 Node.js 贡献者所有。
Node.js 在 MIT 许可证下可用。
.
.Pp
Node.js 还包含在各种许可证下可用的外部库。
完整许可证文本请参见
.Sy https://github.com/nodejs/node/blob/HEAD/LICENSE
.
.Sh 参见
网站：
.Sy https://nodejs.org/
.
.Pp
文档：
.Sy https://nodejs.org/api/
.
.Pp
GitHub 仓库和问题跟踪器：
.Sy https://github.com/nodejs/node
.
.Sh "[中文版维护人]"
.B Aurolystant
.Sh "[中文版最新更新]"
.BR 2026.07.13
.Sh "[中国 Linux 论坛中文手册页翻译计划]"
.BI https://github.com/man-pages-zh/manpages-zh
